# NetDash 릴리스 노트

## v3.68.0 (2026-07-09) — HA(VIP 공유) 방화벽: 토폴로지 이중화 표현 + HA 포트 표기

### 🔗 같은 VIP 방화벽 = 이중화로 표현
- **같은 host(VIP)를 공유하는 방화벽들을 HA 쌍으로 자동 인식** — 이름 패턴 무관(VIP 공유가 확실한 근거).
  - Zone 안에 **나란히 배치** + **이중화 연결선(═)**. Zone 키는 쌍 이름의 공통 토큰(예: OASVR_FGT).
  - **인터넷 방화벽도 HA면 쌍 전체를 중심축에** 표시(참조 구성도의 방화벽#1 ═ 방화벽#2).
- **Backup 중복 링크 제거**: Backup은 VIP로 수집돼 데이터가 Active와 동일 → 같은 상대로의
  연결선은 **VIP 그룹당 1회만** 그림(중복 곡선 방지). 쌍 관계는 이중화 선으로 표현.

### 🔌 이중화 연결선에 HA 포트 표기 (FortiGate)
- 수집 시 **`cmdb/system/ha`에서 HA 구성(hbdev heartbeat 포트·모드) 자동 수집** (기존 세션 재사용).
- 이중화 연결선 라벨에 HA 포트 표시(예: **이중화 ha1·ha2**), 호버 시 heartbeat 포트·모드 툴팁.
- standalone(비 HA)이나 수집 실패 시 기존처럼 "이중화"만 표시(흐름 영향 없음).

## v3.67.1 (2026-07-09) — HA 등록 "FOREIGN KEY constraint failed" 수정 (긴급)

- **v3.67.0에서 Backup 등록 후 Active 등록 시 FOREIGN KEY constraint failed 나던 버그 수정.**
  - 원인: v3.67.0의 UNIQUE 제거 마이그레이션이 `ALTER TABLE ... RENAME`을 사용 →
    SQLite가 자식 테이블(firewall_interfaces/firewall_arp)의 FK 참조를 `firewalls_old`로
    따라가게 바꿔 새 firewalls의 id로 인터페이스 저장 시 FK 실패.
  - 수정: 정석 절차(새 테이블 생성→복사→기존 DROP→RENAME, FK OFF)로 마이그레이션 재작성.
- **v3.67.0으로 이미 깨진 DB 자동 복구**: `firewalls_old` 잔해 제거 + 자식 테이블 FK를
  firewalls로 재구성(secondary_ips 등 후기 컬럼 포함, 데이터 보존). 앱 시작 시 자동 실행.

## v3.67.0 (2026-07-09) — 방화벽 HA/VRRP 이중화: 같은 VIP + 다른 hostname 각각 등록

- **HA/HSRP/VRRP 이중화 방화벽(같은 VIP 하나만 사용)의 Active/Backup을 각각 등록** 가능하게 함.
  - 기존: `firewalls.host`에 UNIQUE 제약 + host 기준 upsert → 두 번째 등록이 첫 장비를 덮어씀.
  - 변경: **(name, host) 복합 키 upsert** — 같은 VIP라도 **hostname이 다르면 별도 등록**.
- 기존 DB의 `host UNIQUE` 제약을 **자동 마이그레이션**(테이블 재구성, 데이터 보존)으로 제거.
- 방화벽 수정 시 같은 host라도 이름이 다르면 허용(더 이상 409 아님).

## v3.66.1 (2026-07-08) — 서버실 엑셀 다운로드: 열별 전체 랙 표기(2개 제한 제거)

- **엑셀에 열(A/B…)의 랙이 2개까지만 나오던 문제 수정.** 예: A열에 A03·A04·A06가 있으면
  이제 **세 랙 모두 한 줄에 나란히** 출력(랙뷰와 동일). 기존 '2개 고정 블록' 방식 제거.
- 열별로 그 열의 **모든 랙을 한 블록에 배치**하고, 열 블록을 세로로 쌓음.
- 컬럼 너비는 한 줄 최대 랙 수에 맞춰 자동 확장.

## v3.66.0 (2026-07-08) — 실측 데이터(MAC/ARP/CONFIG/CDP·LLDP) 기반 직결 판정 강화

- **MAC/ARP/CONFIG 교차대조 직결 링크를 토폴로지 1순위로 승격.** 그동안 CDP/LLDP만 "확정"으로
  Zone을 배정했는데, 이제 **양방향 MAC 확인(mutual) 링크도 실제 직결로 신뢰**해 1순위 배정.
  → 호스트명이 규칙과 달라도(예: 토큰 불일치) **실측 연결로 올바른 Zone/부모에 배치**.
- 링크 표시 3단계: **CDP/LLDP 확정=파란 실선**, **MAC 양방향 직결=청록 실선**,
  **단방향 MAC 추론=회색 점선**. 각 선에 **직결 포트**(local↔remote) 표기.
- 데이터 소스 총동원(이미 반영): 관리 IP + **config의 모든 인터페이스 IP** → ARP 역추적 → MAC 집합
  → 다른 스위치 MAC 테이블에서 **가장 액세스(직결) 성향 포트** 선택 → 실제 직결 포트/장비 도출.
- 범례에 링크 종류(확정/직결/추론) 추가.

## v3.65.0 (2026-07-08) — 존·대역 뷰: 인터넷 스위치 표시 + 등록 장비 누락 방지(미분류 catch-all)

- **인터넷 스위치 표시 수정**: 이름 패턴을 넓히고(`INTERNET`/`INET`/`INT_SW`/`EXT_SW`),
  이름으로 못 찾으면 **인터넷 방화벽에 링크된 스위치**를 자동으로 인터넷 스위치로 인식.
  '구분=L2'로 지정돼 있어도 중심축에 표시.
- **등록 장비 누락 방지(미분류 catch-all)**: 이전엔 미분류가 L3/L4만 표시 → 어느 Zone에도
  배정 못 된 **L2/스위치/방화벽/백본도 모두 '미분류' 박스에 아이콘으로 표시**.
  서버실 L2가 Zone에 안 붙거나 인터넷 스위치가 감지 안 돼도 **더 이상 사라지지 않음**.
  - 미분류 장비는 config 대역이 없으면 **관리 IP의 /24**를 대역으로 표기.
- 참고: 미분류로 나오는 장비는 CDP/LLDP·config **재수집** 후 해당 Zone/중심축으로 편입됩니다.

## v3.64.0 (2026-07-08) — IP 등록 정책 변경: 정적 화이트리스트 → 실제 도달(ping/TCP) 기반

- **정적 IP 화이트리스트 기본 비활성**(`allowed_ip_ranges: []`). 이제 **공인 IP도 등록 가능**
  (예: `208.97.24.195`) — "IP address not in allowed ranges" 오류 해소.
- **등록 게이트를 실제 도달성으로 대체**: 수동 등록 시 **ping(ICMP) 또는 관리 포트(TCP 22·443·23·80)**
  중 하나라도 응답하면 등록. 응답 없으면 명확한 안내 + **강제 등록** 옵션(확인 대화상자).
- **SSRF 안전선은 유지**: loopback / link-local(메타데이터) / multicast / reserved 대역은 항상 차단.
- 자동 수집·collect 재검증·연결 테스트 경로도 동일 정책(공인 허용, 위험 대역 차단).
- 특정 대역만 허용하도록 제한하려면 `config.yaml`의 `allowed_ip_ranges`에 CIDR을 추가.

## v3.63.0 (2026-07-08) — 존·대역 뷰: 이중화 쌍 나란히 배치 + L3 대역 중복 제거

- **L3 밑 대역 중복 버그 수정**: 모든 L3에 같은 대역이 반복 표기되던 문제 해결.
  원인은 Zone 내 L2 /24를 호스트명 토큰으로 매칭할 때 같은 Zone 토큰이라 모든 L3에 전부 붙던 것.
  → 이제 **각 L3는 자기 config(`ip address`) 직결 대역만** 표기, 나머지 L2 대역은 Zone '기타'로 1회만.
- **이중화 쌍 나란히 배치**: 호스트명 이중화 표식(`_M/_B`, `_1/_2`, `BB1/BB2`, `PRI/SEC` 등)을 검출해
  쌍을 **인접 배치**하고 **이중화 연결선(청록)** 으로 이음. L2/L3/L4 역할 토큰은 유지.
- **쌍당 대역 박스 1개**: 정확히 2대인 이중화 쌍(동일 SVI 공유)은 대역 박스를 **하나로 병합**해 중복 제거.
  3대 이상 그룹은 장비별 박스 유지.

## v3.62.0 (2026-07-08) — 인터페이스 MAC 매칭 확장(Phase 2): CDP/LLDP 비활성 장비 링크 커버리지 향상

- **각 장비의 '모든 인터페이스 MAC'으로 물리 연결 추론**(기존은 관리 MAC 1개만 사용).
  - 저장된 running-config의 **모든 `ip address` 인터페이스 IP**를 ARP에서 역추적해 그 장비의 MAC 집합을 구성.
  - 이 MAC들을 다른 스위치의 MAC 테이블과 대조 → 실제 연결 포트를 찾음.
- **효과**: 관리 MAC이 어느 MAC 테이블에도 없던 장비(예: CDP/LLDP 비활성 백본)도
  데이터 인터페이스 MAC으로 **링크·포트가 추론**되어 존·대역 뷰의 점선(추론) 커버리지가 넓어짐.
- CDP/LLDP 확정 링크는 여전히 실선 우선, 이번 확장은 추론(점선) 링크를 보강.

## v3.61.0 (2026-07-08) — 존·대역 뷰: 연결선 + 포트 표시(SVG) + CDP/LLDP 확정 우선

- **HTML 박스(▼) → SVG 다이어그램으로 전환**: 장비 아이콘 사이에 **실제 연결선**을 그리고
  각 선에 **연결 포트**(local ↔ remote)를 표기. (첨부 구성도 스타일)
- **하이브리드 링크**:
  - **CDP/LLDP 확정 링크 = 실선**(파랑, 이중화는 청록) + 정확한 포트
  - **MAC 추론 링크 = 점선**(회색) + 근사 포트(`~` 표시) — CDP/LLDP 미수집/비활성 장비 대응
- **CDP/LLDP 확정 링크 우선 배정**: Zone 소속을 확정 링크(1순위) → 호스트명 토큰 → 보조 링크 순으로
  결정. 중심 OA BB 경계에서 멈추는 BFS로 Zone 간 누수 방지.
- **L2는 아이콘 없이** 각 L3/L4 밑에 **대역 텍스트**로만 표기(그대로 유지).
- 휠=확대 · 드래그=이동 · 노드 클릭=상세 · 선 호버=양단/포트/확정여부 툴팁.
- 참고: 선·포트 정확도는 **CDP/LLDP 재수집** 여부에 좌우됩니다(미수집 구간은 점선 추론).
- **토폴로지를 존·대역 단일 뷰로 통합**: 서버실 구성도 / TPS 구역도 / L2 펼치기 버튼 제거,
  존·대역 구성도 하나로 표시. (기존 렌더 코드는 보존 — 필요 시 복구 가능)

## v3.60.0 (2026-07-08) — 존·대역 뷰: 중심 OA BB / Zone 백본 분리 + 대역 정확도 수정

실제 구성(`인터넷 방화벽 ↔ OA BB`, `각 Zone 방화벽 ↔ OA BB`, `각 Zone 백본 ↔ 해당 Zone 방화벽`)에 맞게 수정.

- **백본 분리**: 모든 백본을 중심축에 쌓던 문제 수정. **중심 OA BB만 중심축**에 두고,
  **Zone별 백본은 해당 Zone 안**(Zone방화벽 → Zone백본 → L3/L4)에 표시.
  - 중심 OA BB = 인터넷 방화벽에 링크된 백본 또는 구별 토큰이 없는 백본(예: VISS).
  - Zone 백본 = 호스트명 토큰이 특정 Zone과 맞는 백본(예: `F1_..BB1` → F1_FA Zone).
- **대역 정확도 수정**: 링크 BFS가 다른 Zone의 L2를 끌어와 대역이 틀리던 문제 수정.
  이제 **L3의 config(`ip address`) 직결 대역만** 사용하고, L2 /24는 **같은 Zone 내부**에서만
  호스트명 매칭으로 추가 → Zone 간 대역 오염 없음.

## v3.59.0 (2026-07-08) — 존·대역 뷰: 방화벽 호스트네임 기반 Zone 구분

- **Zone 분류를 방화벽 호스트네임 토큰 기준(1순위)으로 변경.** IP/링크로 구분이 어려운 환경에서도
  호스트명만으로 Zone을 정확히 분리 (예: OASVR · F1_FA · F2_FA · RC · HUB · Holland · PSUT).
- **공통 접두어 자동 제외**: 모든 방화벽이 공유하는 토큰(예: SKBA 사이트/회사명)은 구분에서 제거,
  구별되는 토큰(OASVR, F1_FA…)만 Zone 키로 사용.
- **이중화 방화벽 자동 병합**: 같은 Zone 토큰의 M/B 쌍(예: OASVR_FW_M + OASVR_FW_B)은 한 Zone으로.
- **링크 미수집이어도 배정**: L3/L4/L2를 호스트명 토큰 겹침으로 소속 Zone에 배정(링크는 보조).
  링크만 있고 이름이 안 맞으면 링크로 배정.
- **L3/L4 밑 대역**: config SVI 대역 + 같은 Zone·같은 토큰 L2의 /24를 그 L3 밑에 표기.
  어디에도 안 붙는 L2 대역은 Zone '기타(직결 L2)'로.
- 백본은 '구분(device_type)=BackBone'을 우선 인식 → **VISS**처럼 이름에 BB가 없는 백본도 지정 시 중심축 표시.

## v3.58.0 (2026-07-08) — 존·대역 뷰(아이콘형 BOX 토폴로지) 추가

### 🗂 존·대역 뷰 — 세 번째 토폴로지 모드 (실제 장비 아이콘)
- **중심축(세로)**: `ISP GW → Internet Switch → Internet FW → OA Backbone SW` 를
  **실제 장비 아이콘**(☁ 구름 / 🔷 스위치 / 🛡 방화벽 벽돌 / ◆ 라우터)으로 표현.
- **Zone별 방화벽 기준 분기**: OA Backbone에 붙은 각 Zone 방화벽 아래 L3/L4를 링크로 추적해
  Zone BOX로 묶음. 경계(Internet) 방화벽은 중심축, Zone 방화벽은 분기로 자동 구분.
- **L2는 아이콘 없이 "대역 정보만"**: 각 L3/L4 밑에 그 장비가 라우팅하는 **대역(CIDR) 목록**만
  표시(config `ip address` + 하위 L2의 /24). 장비가 많아도 깔끔.
- 중심축 특수 장비는 호스트명 패턴으로 식별(ISP/GW, INTERNET SW, INTERNET FW, OA/BACKBONE/BB).
- 방화벽 링크 미수집 장비는 **미분류** BOX로 표시 → 재수집하면 자동 편입.
- 아이콘 카드 클릭 = 장비 상세. 툴바 `🗂 존·대역` 버튼으로 진입.

## v3.57.0 (2026-07-08) — 토폴로지 L2 대역 뱃지 + C9300L 시리얼 수집 수정

### 🌐 토폴로지: L2를 "대역 뱃지"로 접기 (첨부 구성도 스타일)
- 장비가 많은 문제 해결: **L2 스위치 무리를 (상위 L3 → /24 대역)으로 묶어 뱃지 카드 1장**으로 축약.
  카드에 대역(CIDR) · 소속 L2 대수 표시. 방화벽→L4→L3→**대역** 계층으로 깔끔하게 표현.
- **뱃지 클릭 → 하단 드릴다운 패널**에 소속 L2 목록(이름·관리IP·연결대역), 행 클릭 시 상세로 이동.
- 툴바 **`🌐 L2 펼치기/접기` 토글** — 소규모는 개별 노드, 대규모는 대역만.
- 계층 존 박스 정교화: **보안 계층(방화벽)=빨간 실선 강조**, 나머지 색상 점선 + 한글 계층 라벨.

### 🔧 Cisco Catalyst 9300L 시리얼 번호 수집 수정
- **원인**: 9300L은 `show version`에 'System Serial Number' 줄이 없고 하단 스위치 표(Serial No. 열)
  또는 `show inventory`의 SN에만 있음. 기존엔 cisco_ios가 show inventory를 수집하지 않았음.
- **수정**: cisco_ios 수집에 `show inventory` 추가 + 시리얼 패턴 3종 보강
  (inventory의 `SN:`, Catalyst 스위치 표, 기존 System Serial). cisco_xe도 동일 패턴 적용.
- **진단 버튼 개선**: user 모드(`>`)면 자동으로 enable 진입 + `terminal length 0` 설정 후
  `show version`·`show inventory` 수집 → `>`에서 "% invalid input"으로 오해하던 문제 해소,
  파싱된 시리얼도 함께 표시.

## v3.56.0 (2026-07-08) — 서버실 랙 배치도(42U 실물형) + 엑셀 다운로드

- **🗄 서버실 랙 뷰를 실제 42U 랙 배치도로 재설계.** U42(상단)→U1(하단) 전체 프레임을
  그리고, 장비를 지정한 U에 **종류별 색상**으로 배치(방화벽=빨강, Core/백본=보라, L3=진보라,
  L4=주황, L2=청록, 서버=파랑, AP=초록). 빈 유닛은 사선 패턴으로 표시, 범례 추가.
  `location`을 `A03U36` 형식으로 기재한 장비만 해당 랙 U에 표시.
- **⬇ 엑셀 다운로드 버튼 추가.** 서버실 현황 → 랙 배치를 첨부 양식(랙 2개씩 나란히,
  랙명 병합 헤더, U42→U1 행, 장비명 셀 종류색 채움)으로 `.xlsx` 출력.
  엔드포인트 `/api/serverroom/export`(openpyxl).
- 구분(device_type) 미지정 장비는 호스트명 패턴으로 종류를 추론해 색상 적용.

## v3.55.5 (2026-07-08) — 성단 뷰 제거 + 장비 아이콘 전면 재설계

- **🌌 성단 뷰 제거.** 토폴로지는 서버실 구성도 / TPS 구역도 2탭으로 단순화.
- **장비 아이콘을 실제 네트워크 구성도 스타일로 재설계**(더 크고 인지도 높게):
  라우터/백본=원통+사방 화살표, L3=입체 큐브+화살표, L2=납작 입체 박스+양방향(⇄),
  방화벽=벽돌벽, L4=로드밸런서 박스, 서버=랙(슬롯+LED). 채색·입체감 적용, 카드 확대.

## v3.55.4 (2026-07-08) — 구성도 PPTX 버튼을 토폴로지 페이지로 이동

- **📊 구성도 PPTX 버튼을 스위치 현황 → 토폴로지 페이지 툴바로 이동.**
  구성도 생성은 토폴로지 성격이므로 서버실 구성도/TPS 구역도/성단 뷰 버튼과 함께 배치.

## v3.55.3 (2026-07-08) — 전체 코드 감사: 확정 버그 8건 수정

- 9개 차원 병렬 심층 리뷰 + 적대적 재검증(발견 46건 중 8건 확정)으로 다음 수정:
- **[High] Arista MAC/ARP 수집 파손 수정.** 파서가 콜론 형식 MAC만 매칭해
  **Arista 표준 dot 형식(0011.2233.44aa)의 MAC/ARP가 전량 유실**되던 버그 →
  콜론/점/대시/무구분 모두 파싱.
- **[Med] NX-OS err-disabled 포트 색상 오표시 수정.** 'err-disabled'가
  'disabled' 부분일치로 뭉개져 UI에서 회색(정상)으로 보이던 것 → 빨강(위험)으로
  정확히 표시.
- **[Med] rate limit 딕셔너리 메모리 누수(CWE-770) 수정.** 만료된 키가 무한
  누적되던 것 → 512개 초과 시 만료 키 자동 정리.
- **[Med] 토폴로지 window 이벤트 리스너 누수 수정.** 성단 뷰/구성도 재렌더마다
  window 리스너가 쌓이던 것 → 재렌더 시 정리.
- **[Low] 웹 SSH 터미널 자격증명 쿼리 파라미터 제거.** URL 쿼리로 계정을 받던
  미사용 경로 제거(로그 노출 위험 차단) — 저장된 자격증명만 사용.

## v3.55.2 (2026-07-08) — MAC 추론 링크 포트 정밀화(추론 장비도 정확한 토폴로지)

- **CDP/LLDP 비활성 장비의 연결을 MAC 테이블로 정확히 완성.** 지적하신 대로
  추론 장비의 각 포트에 직결된 MAC = 이미 수집한 다른 스위치의 관리 MAC이므로,
  이를 대조해 링크를 그립니다(기존에도 동작하던 로직 + 포트 정밀화).
  - 이웃 MAC이 여러 포트에 보이면(루프·이중화·트렁크 경유) **직결 포트를 선택**:
    물리 포트 우선 + 그 포트의 학습 MAC 수가 가장 적은 것(=액세스/직결 성향).
    트렁크(다수 MAC)로 스쳐 관측된 포트를 직결로 오인하지 않음.
  - 링크에 출처 표시(source=mac/cdp·lldp). 토폴로지 링크 호버 툴팁에
    "CDP/LLDP 확정" 또는 "MAC 추론" 표기.

## v3.55.1 (2026-07-08) — CDP/LLDP 비활성 장비 장비별 자동 폴백·진단 표기

- **CDP/LLDP가 꺼진 장비는 그 장비만 MAC 추론, 나머지는 CDP/LLDP로 정확 수집.**
  - 장비별로 CDP→LLDP 순 시도, 둘 다 비활성(예: 'cdp disabled globally' + LLDP
    미지원)인 장비만 MAC 추론 링크로 폴백(이미 동작하던 로직 + 진단 강화).
  - 스위치 현황 벤더 옆에 **수집 방식 뱃지**: `●CDP`/`●LLDP`(정확) 또는
    `⚠추론`(비활성 — 마우스 올리면 사유). 어느 장비가 추론인지 한눈에 확인.
  - 수집 방식·사유를 로그(neighbor_source)에도 기록. 장비 설정은 변경하지 않음(읽기전용).

## v3.55.0 (2026-07-08) — 네트워크 구성도 PPTX 자동 생성(SK AX 브랜드)

- **고객 전달용 네트워크 구성도 PPTX를 자동 생성.** 스위치 현황의
  **📊 구성도 PPTX** 버튼 → 고객사명·생성일자 입력 → 다운로드.
  - 슬라이드: **표지(SK AX USA 로고+고객사명+일자) → 서버실 구성도(코어) →
    TPS 구역 개요(구역별 장비수·상태 롤업) → 구역별 상세(구역당 1장, 40대↑ 자동 분할)**.
  - 다이어그램은 수집 데이터(+CDP/LLDP 연결)로 **자동 배치·렌더**(장비 아이콘·
    종류색·상태색·포트 라벨). 요약 통계(정상/경고/장애) 텍스트 포함.
  - 파일명: `고객사명_네트워크구성도_YYYYMMDD.pptx`. SK AX USA 로고·브랜드 컬러 적용.
  - 폐쇄망 안정성: Cairo/Graphviz 없이 **Pillow + python-pptx**(순수 파이썬)로 구현.
  - ※ 빌드 PC에 python-pptx·pillow 필요(requirements 반영).

## v3.54.0 (2026-07-07) — CDP/LLDP 이웃 수집(정확한 물리 연결) — PPTX 자동화 1단계

- **CDP/LLDP neighbor 수집 추가** — MAC 추론이 아니라 **프로토콜이 알려주는
  정확한 물리 연결**(로컬포트↔원격장비/원격포트).
  - Cisco IOS/NX-OS: `show cdp neighbors detail`(+LLDP 폴백),
    Arista/ExtremeXOS: `show lldp neighbors detail(ed)`.
  - 새 `neighbors` 테이블에 저장(스위치별 교체).
- **토폴로지 링크 정확도 대폭 향상.** CDP/LLDP로 확인된 링크는 **양쪽 포트가
  확정**되어 MAC 추론 링크 위에 덮어씀(source=cdp/lldp). 서버실 구성도·성단 뷰가
  실제 배선대로 그려짐.
- (다음 단계: 이 정확한 연결 데이터로 고객 전달용 PPTX 구성도 자동 생성)

## v3.53.0 (2026-07-07) — 토폴로지 🌌 성단 뷰(포스 그래프)

- **토폴로지에 '성단 뷰' 모드 추가** — 자비스st 은하 파티클 컨셉.
  - 다크 배경 + **네온 글로우 노드**(종류색) + 반투명 곡선 링크.
  - **포스 그래프**: 연결된 장비끼리 서로 끌어당겨 **구역이 자연스럽게 성단으로
    형성**(백본 주위에 액세스가 뭉침). 라이브러리 없이 Canvas + 순수 JS 시뮬레이션.
  - **호버 포커스**: 마우스를 올린 장비와 그 직결 링크만 선명, 나머지는 흐려짐 —
    장비가 수백 대여도 관계를 즉시 식별. 툴팁에 이름·IP·직결 수·대역.
  - 드래그=이동, 휠=확대, 클릭=상세보기. 노드 크기=연결 수(중요도).
- 기존 서버실 구성도(3-Tier)·TPS 구역도와 버튼으로 전환.

## v3.52.0 (2026-07-07) — 접근로그 이더넷 IP · 알람 포트 표기 · config 선택 다운로드

- **접근 로그의 127.0.0.1을 이더넷 IP로.** 같은 PC(루프백) 접속이면 서버의
  이더넷 IP를 조회해 그 IP로 기록(프록시 뒤 X-Forwarded-For는 그대로 우선).
- **루프/플래핑 알람에 문제 포트 표기.** "플래핑(flapping) 감지: SW01 [Eth1/9, Gi1/0/24]"
  처럼 어느 포트가 문제인지 로그 분석에서 추출해 함께 표시.
- **config 다운로드 = 선택 장비만.** 버튼명을 'config 일괄 다운로드'→'config 다운로드'로
  바꾸고, 체크박스로 **선택한 스위치만** ZIP 다운로드(미선택 시 확인 후 전체).

## v3.51.0 (2026-07-07) — NX-OS 포트 상태 오인식 수정 + 시간 EST 표시

- **UP 포트가 DOWN으로 표시되던 문제 수정(N9508 등 대형 섀시).**
  - 원인 ①: 포트가 많은 장비는 `show interface`(전체) 출력이 매우 커서 60초
    타임아웃으로 실패 → `show interface status` 폴백으로 넘어감.
  - 원인 ②: 폴백 파서가 **Name/설명 컬럼의 단어**(예: 설명 `LINK-TO-DOWNSTREAM`의
    'DOWN')를 포트 상태로 오인식.
  - 수정: 폴백을 **컬럼 기반**(상태는 VLAN 토큰 바로 앞)으로 정확히 파싱 +
    `show interface`/errors 명령 타임아웃을 150초로 확장(대형 섀시 대응) +
    포트명 축약 통일(`Ethernet1/9`→`Eth1/9`, 파서 간 일치).
- **모든 시간 표시를 EST(America/New_York)로.** DB는 UTC로 저장되는데 시간이
  틀리게 보이던 문제 → UTC로 해석 후 EST로 변환해 표시(서머타임 자동 반영, 'EST' 표기).

## v3.50.0 (2026-07-07) — 토폴로지 3-Tier 구성도 + 계층 자동 추론

- **서버실 구성도를 표준 3-Tier 아키텍처 형태로.** 계층 라벨을
  **Firewall(Gateway) → Core Layer(백본) → Distribution Layer(L3·L4) →
  Access Layer(L2)** 로 표기(첨부 구성도 형식). L3·L4를 Distribution 계층으로 통합.
- **구분(device_type) 미지정 시 hostname 자동 추론.** 이름 패턴으로 계층 분류:
  FABB→Core, FASW→Access, OASVR/L4→L4, _FW→Firewall, CORE/BB→Core 등.
  구분을 일일이 지정하지 않아도 구성도가 계층으로 나뉘어 그려짐(구분을 직접
  지정하면 그 값이 우선).
- 범례를 Core/Distribution/Access 명칭 + 종류 심볼로 갱신.

## v3.49.1 (2026-07-07) — NX-OS 포트 상태·속도·오류 정확도 수정(show interface 기반)

- **NX-OS에서 down 포트가 up으로 표시되고 속도·CRC·IN/OUT 오류가 안 잡히던
  문제 수정.** 원인: `show interface brief/status`는 실제 링크 상태·오류 카운터를
  담지 않음.
  - 수정: 주 수집 명령을 **`show interface`(전체)** 로 전환 —
    `EthX/Y is up/down (사유)`에서 **실제 링크 상태**, `10 Gb/s`에서 **속도**,
    `N CRC` / `N input error` / `N output error`에서 **CRC·수신·송신 오류**를 파싱.
    VLAN은 `show interface status`에서 병합. detail이 없으면 status→brief 폴백.
  - 예: SFP 미삽입으로 down인 포트가 이제 정확히 'down'으로 표시.

## v3.49.0 (2026-07-07) — 웹 SSH 터미널(PuTTY형) 추가

- **브라우저에서 장비에 직접 SSH 접속.** 스위치 현황의 💻 버튼 또는 상세 패널의
  "SSH 터미널" 버튼으로 xterm.js 기반 실시간 터미널을 열어 `show run`·`ping`·
  `show log` 등 수집 정보 외 명령을 직접 확인.
  - 저장된 계정(세션/DPAPI) 자동 사용, 실시간 입출력(WebSocket), 창 크기 반영.
  - 보안: 등록 IP만(SSRF 재검증), 원격 접속은 토큰 필요(로컬은 면제),
    세션 open/close 감사 로그, 유휴 10분·최대 1시간 자동 종료.
  - ※ 실제 장비에 명령이 직접 전달되니 주의. (빌드 PC에 flask-sock·
    simple-websocket 필요 — requirements.txt 반영)

## v3.48.0 (2026-07-07) — NX-OS VLAN·속도·CRC/오류 수집 수정

- **Cisco NX-OS 포트의 VLAN·속도·CRC·IN/OUT 오류 수집 안 되던 문제 수정.**
  - 원인: NX-OS 파서가 `show interface brief`만 쓰면서 **VLAN=1·속도=unknown으로
    하드코딩**하고 오류 카운터는 수집조차 안 했음.
  - 수정: 수집 명령에 **`show interface status`**(VLAN·속도 표)와
    **`show interface counters errors`**(FCS=CRC, Rcv=수신오류, Xmit=송신오류)
    추가하고 파서가 실제 값을 추출. brief는 up/down 폴백으로 유지.

## v3.47.0 (2026-07-07) — 방화벽 L3 링크 인식 + 세그먼트 구역 박스

- **방화벽↔스위치 링크 누락 수정(FABB↔F1_FA_FW 케이스).**
  - 원인: 방화벽을 스위치 1대에만 연결하고, MAC이 안 잡히면 링크를 아예 안 그림.
    방화벽이 백본에 **L3로 직결**되면 L2 MAC이 안 보여 누락됐음.
  - 수정: ① 방화벽 MAC이 관측된 **모든 스위치에 다중 연결** ② **L3 인접 폴백** —
    스위치가 라우팅하는 대역(config)에 방화벽 IP가 포함되면 링크 생성.
    L3 링크는 **파란 점선**, 관측 링크는 실선으로 구분.
- **세그먼트(구역) 박스.** 첨부 구성도처럼 계층마다 **라운드 사각형 컨테이너 +
  라벨**(방화벽 Gateway / 코어·백본·L3 / L4 / L2·액세스)로 감싸 한눈에 구역 구분.

## v3.46.0 (2026-07-07) — 토폴로지 구성도화(실제 심볼 + 이중화 계층 + 전체화면)

- **실제 네트워크 구성도 형식.** 색 박스 대신 **장비 심볼(SVG)**: 방화벽=벽돌벽,
  라우터/L3=원형+방향, L4=육각형, L2=포트 박스, 서버=서버 아이콘.
- **이중화 코어 인식.** 같은 계층에서 서로 직접 연결된 두 장비를 **쌍으로 나란히
  배치 + 굵은 청록 교차선**(이중화 링크)으로 표현. 아래로 부채꼴 분배.
- **링크 노이즈 제거.** 서버실 구성도는 **양방향 확인(mutual)된 링크 + 방화벽
  링크만** 표시(dynamic MAC 한쪽 관측 가짜 링크 제거). 단일 관측 링크는 점선.
- **전체 화면 표시.** 구성도가 캔버스 전체 높이를 채우도록(상단만 보이던 문제
  수정) — viewBox 자동 맞춤(preserveAspectRatio).
- 포트 라벨·연결 대역·인터페이스 IP는 노드/링크 호버 툴팁.

## v3.45.0 (2026-07-07) — 토폴로지 2탭(서버실 구성도 / TPS 구역도) + 가시성 개선

- **가시성 우선 재설계**: 확대 없이 읽히도록 노드를 **중간 카드**(종류 아이콘 +
  호스트명 1줄 + IP + 상태 점)로 크게. 좌측 종류색 띠 + 대역 개수 뱃지.
- **[🏢 서버실 구성도] 탭**: 방화벽·백본·L3·L4만 계층 배치
  (방화벽 → 백본/L3 → L4 → L2). **장비 종류를 아이콘·색으로 구분**
  (🛡방화벽 빨강 / ◆백본·L3 보라 / ▧L4 주황 / ▢L2 청록). 연결선에 **포트 라벨**,
  노드에 마우스 올리면 **연결 대역·인터페이스 IP**(방화벽 primary/secondary)·포트.
- **[🏭 TPS 구역도] 탭**: **구역 드롭다운**으로 한 구역씩(20~30대) 표시 —
  한 화면에 너무 많이 그리지 않아 안 엉킴. 상위 코어는 점선 고스트로 업링크
  방향 표시(클릭 시 서버실 탭으로 이동).
- **연결 대역 태그**: 백본/L3 노드에 그 스위치가 라우팅하는 대역을 표시
  (이미 저장된 running-config 백업에서 파싱 — 추가 장비 접근 없음).
- 종류 구분은 '구분'(device_type) 컬럼 우선, 없으면 hostname 패턴 추론.

## v3.44.0 (2026-07-07) — 방화벽 인터페이스 Primary/Secondary IP 통합 표기 + Prefix

- **secondary IP를 같은 인터페이스 행에 통합.** 별도 '(2nd)' 행 대신
  port1의 IP 칸에 **Primary IP(위) / Secondary IP(아래, 파란색 2nd 뱃지)** 로
  스택 표시.
- **마스크를 Prefix로 표기.** 넷마스크(255.255.255.0)가 아니라 **/24** 형식으로
  통일(전 인터페이스, secondary 포함).

## v3.43.0 (2026-07-07) — 방화벽 secondary IP 수집 + 설비 '포트 설명' 표기

- **FortiGate secondary interface IP 수집.** cmdb의 `secondaryip` 목록을 읽어
  방화벽 인터페이스 목록에 `port1 (2nd)` 행으로 추가(상세 모달에서 파란 '2nd'
  뱃지로 구분). monitor의 기본 IP와 병합해 함께 표시. (PaloAlto는 논리
  인터페이스 목록에 이미 모든 IP가 나열됨)
- **설비 현황에 '포트 설명' 컬럼 추가.** 설비가 직결된 스위치 포트의
  **Description**(연결 스위치에서 수집)을 함께 표기 — 예: 'CCTV-정문',
  'AP-3F-회의실'. 수집된 설비가 무엇인지 포트 설명으로 바로 식별.
  통합 검색·엑셀 추출에도 포트 설명 포함. 재수집/새로고침(재매칭) 시 자동 반영.

## v3.42.0 (2026-07-07) — 토폴로지 전면 개편: 구역 집계 맵 ↔ 구역 상세(2단계)

- **1단계 — 구역 집계 맵(기본 화면)**: 장비를 전부 그리지 않고 **구역 카드**
  (🏢 백본 / 1공장 A동 2층 ...)로 묶어 표시. 카드에 스위치·방화벽 수와 상태
  요약(✅ 정상 / ⚠ 경보 / 🔴 문제 N대), **문제 장비가 있는 구역은 빨간 테두리**
  — 최상위에서 장애 위치 즉시 식별. 구역 간 선은 실제 물리 링크 집계
  ("3링크", 호버=실제 장비 쌍 목록).
- **2단계 — 구역 클릭 = 내부 장비 트리**: 그 구역 장비만 업링크(위)→다운링크
  (아래) 계층으로 표시. **다른 구역의 직결 장비(백본 등)는 점선 고스트**로
  업링크 방향을 보여주고, 클릭하면 그 구역으로 이동. 자비스식 호버
  (직결 라인 글로우 + 무관 요소 페이드) 유지. 빵부스러기(전체 > 1공장...)로 복귀.
- **장비 검색**: 이름/IP 입력 후 Enter → 해당 구역이 자동으로 열리고 그 장비가
  주황 링으로 강조.
- **방화벽도 토폴로지에 포함**: 스위치 ARP/MAC에서 방화벽 직결 스위치·포트를
  추론해 🛡 노드+링크로 표시. 설비(단말)는 포함하지 않음 — 스위치·방화벽 전용.

## v3.41.0 (2026-07-07) — 접근로그 실IP · 방화벽 연결 감시 · 끊김 식별 · 토폴로지 하이라이트

- **접근 로그 실사용자 IP 기록**: X-Forwarded-For/X-Real-IP 헤더 우선 —
  프록시/포워딩 환경에서 127.0.0.1만 찍히던 문제 해결.
- **'마지막 수집' 컬럼 버그 수정**: 수집 성공 시각이 저장되지 않아 항상 비어
  있었음 → 이제 성공할 때마다 갱신(실패 시엔 마지막 '성공' 시각 유지).
  '경보' 컬럼은 루프/플래핑 등 로그 분석에서 이상 감지 시에만 채워지는 정상 동작.
- **방화벽 연결 상태 감시**: 도달성 감시가 방화벽(관리 포트 TCP)도 확인 —
  방화벽 현황 표에서 인터페이스/ARP 컬럼 제거(상세에서 확인) 대신
  **'연결 상태'(🟢 연결됨/🔴 끊김/확인 중)** 컬럼. 끊김/복구 시 알람 이벤트.
- **현황판·서버실 끊김 식별**: 방화벽 카드에 '🔴 연결 끊김' 배지 + 카드/랙
  유닛 위험색(스위치는 기존 '🔴 도달불가' 배지 유지).
- **주기 모니터링(부하 관점)**: 60초마다 TCP 핸드셰이크 1회(SYN→ACK→종료,
  로그인 없음) — 장비 CPU 관여 없이 커널이 응답해 부하 사실상 0.
  동시 10대 제한+50ms 간격 분산. 간격은 설정(reach_check_interval)으로 조정 가능.
- **토폴로지 가시성**: 노드에 마우스를 올리면 **직결 라인이 청록 글로우로
  선명해지고 나머지 노드/라인은 흐려짐** — 어느 장비가 어디에 붙어 있는지
  한눈에 식별. 직결 이웃 노드 테두리도 강조.

## v3.40.0 (2026-07-07) — 시리얼 번호 수집·표기 + Alteon 버전 소스 보강

- **시리얼 번호 자동 수집.** 수집 시 벤더별 소스에서 추출해 저장:
  - Cisco IOS/IOS-XE: show version `Processor board ID`/`System Serial Number`
  - Cisco NX-OS: show inventory `SN:` · ExtremeXOS: show version 시리얼 필드
  - Arista `Serial number:` · Alteon Serial 표기 + 범용 패턴
- **스위치 현황에 '시리얼' 컬럼 추가**(버전 옆). 통합 검색에도 시리얼 포함 —
  시리얼로 장비 검색 가능. 엑셀 보고서 스위치 시트에도 시리얼 컬럼.
- **Alteon 버전 미수집 장비 수정.** 일부 장비는 버전이 `/info/sys/general`에
  없음 → **`/boot/cur`을 수집·진단에 추가**하고 booted software version 패턴
  파싱. 진단은 두 명령을 연속 실행해 원문 표시.
- 진단(자동 교정)도 시리얼까지 저장: "→ 모델/버전 저장: 5224 / Alteon 32.4.15.0 / SN:..."

## v3.39.5 (2026-07-07) — 진단이 모델·버전까지 저장(장비 간 표기 불일치 해소)

- **같은 Alteon인데 어떤 장비는 '5224 / Alteon 32.4.15.0', 어떤 장비는 벤더
  'Radware'만 보이던 불일치 해소.** 원인: 수집까지 끝난 장비만 모델/버전이
  채워지고, 진단으로 벤더만 교정된 장비는 빈 채로 남았음.
  → **진단이 받은 `/info/sys/general` 원문에서 모델·버전까지 파싱해 즉시 저장**
  ("→ 모델/버전 저장: 5224 / Alteon 32.4.15.0" 안내 표시).
- 물론 **재수집 한 번**으로도 동일하게 채워집니다(MAC/ARP/포트 데이터 포함 —
  재수집 권장).

## v3.39.4 (2026-07-07) — 진단도 Alteon 명령(/info)으로

- **진단 기능이 Alteon 프롬프트를 인식하면 `show version` 대신
  `/info/sys/general`을 실행** — 진단 화면에서 실제 모델/버전 원문을 바로 확인.
- 참고: Alteon 수집 자체는 처음부터 메뉴 경로 명령만 사용합니다 —
  `/info/sys/general`(버전·모델) · `/info/l2/fdb/dump`(MAC) ·
  `/info/l3/arp/dump`(ARP) · `/info/link`(포트) · `/cfg/dump`(설정 백업).
  `show version`은 벤더를 모를 때의 탐지 프로브에만 쓰입니다(오류 응답이어도
  프롬프트로 판별).

## v3.39.3 (2026-07-07) — Alteon 'Standard ADC' 실장비 프롬프트 반영(진단 원문 기반)

- **실장비 진단 원문(`>> ... - Standard ADC - Main#`) 기준으로 인식 확정.**
  - `- Main#` 꼬리 / `Standard ADC` 키워드로 프롬프트·출력·버전 감지 확장
    ('>>' 접두가 잘려 캡처돼도 인식).
  - **프롬프트 감지 즉시 벤더를 alteon으로 저장** — 이번 재수집이 (관리 세션
    제한 등으로) 실패해도 **다음 수집부터는 처음부터 Alteon 경로(로그인 1회)**.
  - 전용 수집 전 3초 대기(직전 세션 정리 — 연속 로그인 거부 회피).
- **진단 버튼이 벤더 자동 교정까지 수행.** 진단 결과에서 벤더가 확인되면 DB를
  바로 교정하고 "→ 벤더를 'alteon'으로 자동 교정했습니다. 이제 재수집하세요."
  안내 표시 — 수집 없이 진단 한 번으로도 경로가 잡힘.

## v3.39.2 (2026-07-06) — Alteon 직행 폴백 + '진단' 버튼(실응답 원문 확인)

- **Alteon 감지 시 전용 수집 '직행'.** 구형 Alteon은 동시 관리 세션 수 제한이
  있어, 프롬프트 감지 후 추가 로그인(프로브)이 거부되며 실패했을 가능성 →
  프롬프트에서 Alteon이 확인되면 **프로브 없이 곧바로 전용 메뉴 CLI 수집** 실행.
- **스위치 현황에 '진단' 버튼 추가.** 장비의 **실제 로그인 배너·프롬프트·
  show version 응답 원문**을 모달로 표시(TCP/SSH 성공 여부, 감지 결과 포함).
  벤더가 계속 '알 수 없음'이면 이 화면 내용을 복사해 전달 —
  실제 응답 형태를 보고 인식 패턴을 정확히 맞출 수 있음.

## v3.39.1 (2026-07-06) — Alteon 인식 2차 수정(프롬프트 직접 검사)

- **Alteon L4가 여전히 '알 수 없음'으로 남던 문제 수정.**
  - 원인: netmiko는 명령 결과에서 **프롬프트를 잘라내고 반환**하므로, 출력 텍스트에
    `>> Main#` 시그니처가 남지 않아 v3.39.0의 출력 기반 탐지가 통과되지 못했음.
  - 수정: ① 접속 직후 **프롬프트 자체**(`>> ... Main#`)를 검사해 Alteon이면 즉시
    폴백 ② 원시 셸 프로브 출력(프롬프트 포함)에서도 Alteon 판별
    ③ 프로브가 alteon으로 판정하면 **전용 메뉴 CLI 수집기로 수집**.
  - 결과: unknown으로 등록된 Alteon도 한 번 수집이면 벤더 Radware ·
    모델 5224 · 버전 Alteon 29.x가 채워짐.

## v3.39.0 (2026-07-06) — Alteon 자동 인식 · 벤더 표기 간소화 · NX-OS 모델 · DB 락/limit 수정

- **Alteon L4(5224 등) '알 수 없음' 수정.** Alteon 프롬프트(`>> Main#`)가 `#`로
  끝나 cisco 접속이 '성공'하고 명령은 오류 텍스트만 받아 예외 없이 끝나던 문제 →
  출력에서 **Alteon 시그니처를 탐지하면 전용(메뉴 CLI) 수집으로 즉시 재수집**하고
  벤더를 alteon으로 교정. 모델(5224)·버전(Alteon 29.x)까지 표기.
- **벤더 컬럼 표기 간소화**: `Cisco / Extreme / Arista / Juniper / Radware`처럼
  **순수 벤더명만** 표기(OS 구분은 버전 컬럼: IOS-XE 17.x / NX-OS 9.x가 담당).
- **NX-OS 모델명 미표기 수정**: `show inventory`를 수집에 추가하고 **PID**에서
  모델을 파싱(표기 변형 무관, 가장 확실한 소스). 재수집 한 번이면 채워짐.

## v3.38.5 (2026-07-06) — 'database is locked' 수집 중단 수정 + 설정 변경 rate limit 완화

- **수집 중 'database is locked'로 중단되던 문제 근본 수정.**
  - 원인: SQLite 기본 설정(잠금 대기 5초 + DELETE 저널)에서 수집 워커·설비 수집·
    도달성 체크·화면 조회가 동시에 겹치면 잠금 충돌 발생.
  - 수정: **WAL 저널 모드**(읽기와 쓰기가 서로 차단하지 않음 — 경합 자체 감소)
    + **잠금 대기 30초**(즉시 실패 대신 대기). 스레드 8개 동시 읽기/쓰기 240회
    스트레스 테스트로 검증. ※ DB 폴더에 netdash.db-wal/-shm 파일이 생기는 것은
    정상입니다(같이 보관, 삭제 금지).
- **스위치 현황 설정 변경 시 'rate limit exceeded' 수정.** 구분 인라인 변경(PUT)이
  분당 20회 제한이라 연속 편집 시 차단 → 240회/분으로 상향(삭제·일괄 변경·수동
  등록 등 편집 계열도 함께 현실화).

## v3.38.4 (2026-07-06) — 설비 대역 삭제 + 상태 컬럼 제거

- **설비 현황 대역 삭제 기능.** 대역 필터에서 대역을 고르면 옆의
  **"선택 대역 삭제"** 버튼이 활성화 — 그 대역의 수집 결과를 한 번에 삭제
  (예: 겹쳐 수집된 `10.92.174.0/23` · `10.92.174.0/26` 중 하나 정리). 확인 후 삭제.
- **설비 현황 표에서 '상태' 컬럼 제거.** 온라인 배지는 정보량이 적어 삭제하고,
  오프라인(연결 실패)은 행 배경(옅은 빨강) + 툴팁으로만 신호. 엑셀/TXT 추출에는
  상태가 계속 포함됩니다.

## v3.38.3 (2026-07-06) — 서버실 카드 뷰를 현황판과 동일한 배치로

- 서버실 현황 **카드 뷰**를 현황판 카드 뷰와 **동일한 평면 그리드 배치**로 변경
  (랙 섹션 제목/U 배지 래퍼 제거). 정렬은 랙 오름차순 → U 내림차순 유지.
  열(A/B)·U 구조가 필요하면 **랙 뷰**를 사용하세요.

## v3.38.2 (2026-07-06) — 설비 대역 수집 VRF 지원(완료인데 0대 수정)

- **대역 수집이 '완료'인데 리스트에 안 나오던(0대) 문제 수정 — VRF 지원.**
  - 원인: 관리 IP(10.92.x)와 다른 수집 대역(172.27.x)이 **VRF**에 속하면
    일반 `ping`은 글로벌 라우팅으로 나가 실패하고, `show ip arp`(글로벌)에도
    그 대역이 보이지 않아 조용히 0대로 완료됐음.
  - 수정: 수집 시작 시 `show vrf` + `show ip route vrf X connected`로
    **대역의 VRF를 자동 탐지**(하위 대역 포함 매칭) → `ping vrf X ...`,
    `show ip arp vrf X`로 자동 전환. 진행 표시에 "(VRF X)" 표기.
  - 0대로 끝나면 ARP 원문 샘플을 로그에 남겨 즉시 진단 가능.

## v3.38.1 (2026-07-06) — FortiGate 429 수정 · 대역 자동 찾기 보강 · 아이콘 통일

- **FortiGate 수집 429(Too Many Requests) 수정.** 원인: 수집 1회에 로그인 세션을
  2개(인터페이스용+ARP용) 만들어 FortiGate 속도 제한에 걸림 →
  **세션 1개 재사용(로그인 1회)** + 429 응답 시 **Retry-After 대기 후 자동 재시도**.
- **설비 '대역 자동 찾기' 보강.** IOS-XE에서 대역을 못 찾던 문제:
  route/interface 출력 외에 **running-config의 `ip address <IP> <마스크>`** 를
  3번째 소스로 추가(L2 SVI·VRF 환경에서도 확실). 못 찾으면 출력 샘플을 로그에
  남겨 형식 차이를 추적 가능하게.
- **서버실 카드 뷰 방화벽 아이콘 통일**: 검은 🛡 큰 아이콘 대신 스위치와 동일한
  포트 그리드 아이콘(상태색 반영), 이름 앞 🛡 표시로 구분.

## v3.38.0 (2026-07-06) — 설비 대역 청크 수집(/23 대응) · 서버실 열 정렬 · IP 정렬

- **설비 대역 수집을 '청크 방식'으로 재설계** — /24에서 "완료인데 빈 결과"가 나오고
  /26만 되던 문제의 근본 대책, /23 이상도 안정 수집:
  - **ping 32개마다 그 자리에서 ARP를 중간 수집·누적** — 맨 끝 1회 조회에 전부를
    걸지 않아, 도중에 무엇이 끊겨도 그때까지의 결과는 확보(부분 완료 표시).
  - **ping 사이 50ms 간격(pacing)** — 스위치 제어평면 부담 완화(/23 기준 +26초뿐).
  - 진행 표시에 "ARP 중간수집 n회 · 확보 m대" 실시간 표기. 대역 밖 ARP는 제외.
- **서버실 랙 뷰 열(row) 정렬**: A01 A02...가 한 줄, B01 B02...가 다음 줄
  ("A 열"/"B 열" 제목), 각 랙 안은 **U 내림차순(U42→U41→U40)**. 카드 뷰도
  랙별 섹션(U 내림차순 + U 배지)으로 그룹화.
- **설비 현황 IP 정렬**: 숫자 기준 정렬(10 < 2로 꼬이던 문자열 정렬 교정) +
  IP 헤더 클릭으로 오름/내림차순 전환(▲/▼).

## v3.37.0 (2026-07-06) — 벤더 표기 통일 · 현황판 IP 검색 · 버전 백필

- **벤더 값/표기 전면 통일.** 표(extreme_exos)와 수정 모달(extreme)이 다르던 문제:
  - 저장 값을 표준(cisco_ios/cisco_nxos/arista_eos/extreme_exos/alteon)으로 통일 —
    수동 등록·수정 시 자동 정규화 + **기존 DB도 시작 시 일괄 정규화**.
  - 화면 표기는 통일 라벨(Cisco IOS/IOS-XE, Cisco NX-OS, Extreme EXOS...)로,
    수정 모달 드롭다운도 같은 표준 값 사용 — 어디서 봐도 동일.
- **현황판 검색에 IP 포함**: 위치·호스트네임뿐 아니라 IP·이름·모델로도 검색.
- **현황판 카드에 모델·버전 표시**: 하단 상태줄이 벤더 대신
  `모델 · 버전 · 상태`(모델 없으면 벤더 라벨)로 표기.
- **버전 자동 백필**: 버전이 빈 IOS/NX-OS 스위치를 **기존 config 백업의 version
  줄**로 시작 시 자동 채움(재수집 불필요). 그 외(모델, EXOS 등)는 이 버전으로
  **한 번 재수집**하면 채워짐 — 빈 칸('-')에 안내 툴팁 추가.

## v3.36.4 (2026-07-05) — NX-OS/EXOS 모델·버전 인식 수정

- **EXOS 모델이 절대 안 나오던 원인 수정**: 모델(System Type:)은 `show version`이
  아니라 **`show switch`** 에 있음 → EXOS 수집에 `show switch` 추가하고 모델을
  그 출력에서 파싱.
- **폴백(원시 셸 프로브) 경로로 수집된 장비의 버전/모델 누락 수정**: 프로브가 읽은
  show version 원문을 버전/모델 파싱에 재활용 + 재수집 경로에서도 확보.
- **NX-OS 패턴 보강**: 구형(kickstart/system 표기) 버전, `N9K-...`식 모델 표기 추가.
- 적용: 해당 장비를 **한 번 재수집**하면 모델/버전이 채워집니다.

## v3.36.3 (2026-07-05) — 설비 대역 수집 'socket is closed' 수정(자동 재접속)

- **대역 수집 중 세션이 끊기면(socket is closed) 자동 재접속 후 이어서 진행.**
  - 원인: /23 대역은 ping 510회에 15분+ 세션이 필요한데, 장비 세션 시간 제한이나
    중간 방화벽의 유휴 세션 정리로 TCP가 끊기면 전체 수집이 실패했음.
  - 수정: **SSH keepalive(15초)** 활성화 + 끊김 감지 시 **자동 재접속(최대 5회)
    후 끊긴 지점부터 재개**(진행률 보존, IP 누락 없음). 진행 표시에
    "세션 끊김 — 재접속 n/5 (진행 x/y)" 안내. ARP 조회 단계에서 끊겨도
    재접속해 ARP만 재시도(스윕 결과 유지).

## v3.36.2 (2026-07-05) — 스위치 현황 컬럼 필터 행 완전 제거

- 스위치 현황의 **컬럼 필터 행 전체 제거** — 구분/벤더/상태/경보 드롭다운과
  '초기화' 버튼까지 모두 삭제. 검색·필터는 **우측 상단 통합 검색창 하나**로 통일
  (IP·호스트네임·모델·버전·위치·구분·벤더 전 컬럼 검색).

## v3.36.1 (2026-07-05) — 검색 일원화 + VLAN 로딩 표시

- **스위치 현황 검색 일원화.** 컬럼별 텍스트 검색칸 제거 — 우측 상단 **검색창 하나**로
  IP·호스트네임·모델·버전·위치·구분·벤더 등 전 컬럼 통합 검색.
  드롭다운 필터(구분/벤더/상태/경보)는 유지, "초기화"는 검색창까지 함께 비움.
- **VLAN 현황 로딩 표시.** 스위치가 많으면 계산에 수 초 걸릴 수 있어
  "계산 중..." 안내를 표시(빈 화면으로 오해 방지).

## v3.36.0 (2026-07-05) — 스위치 컬럼별 필터 + 설비 대역 필터/통합 검색

- **스위치 현황 컬럼별 필터.** 헤더 아래 필터 행 —
  구분/벤더/상태/경보는 **드롭다운**(현재 값 기준 자동 구성), IP/호스트네임/모델/
  버전/위치는 **텍스트 검색**. 조건은 AND로 결합, 입력 즉시 반영, "초기화" 버튼.
  필터된 리스트에서 체크→일괄 삭제/구분 변경도 그대로 동작.
- **설비 현황 대역 필터 + 통합 검색.**
  - **대역 드롭다운**: 등록된 대역 목록(건수 표시)에서 선택 → 그 대역만 표시.
  - **통합 검색**: 호스트 IP · 네트워크 대역 · **MAC(콜론/점/대시/무구분 형식 무관)** ·
    연결 스위치/포트 어느 값으로도 검색. 요약에 필터 결과 건수 표시.

## v3.35.0 (2026-07-05) — 구분(장비 유형) 분류 + 모델/버전 분리 컬럼

- **'구분' 컬럼을 장비 유형 분류로 개편.** 이름(호스트네임과 중복) 대신
  **BackBone / L3 Switch / L2 Switch / L4 Switch / Server / Firewall / AP /
  Tablet / PC / 기타** 중에서 선택하는 **드롭다운**으로 변경(선택 즉시 저장).
  - **일괄 변경**: 체크박스로 여러 대 선택 → 상단 "구분 일괄변경…" 선택 →
    "선택 항목에 적용"으로 이미 등록된 장비들도 한 번에 분류.
  - 수동 추가/수정 모달에도 구분 선택 추가.
- **모델과 버전을 별도 컬럼으로 분리** 표기(기존 2줄 스택 → 각각 한 컬럼).

## v3.34.1 (2026-07-05) — '수집중' 박제 복구 + unknown 벤더 미갱신 수정

- **재시작 후에도 '수집중'으로 남던 박제 상태 복구.** 이전 실행이 수집 도중
  중단(강제 종료 등)되면 DB에 '수집중'이 남아, 실제로는 아무것도 돌지 않는데
  영원히 수집중으로 보였음(수동 수집 시에만 해소) → **앱 시작 시 자동으로
  '실패(중단됨 — 재수집 필요)'로 정리**해 오류 탭에서 바로 재수집 가능.
- **수집 성공했는데 벤더가 unknown으로 남던 버그 수정.** unknown 장비가
  IOS-XE 등으로 감지돼도, 접속용 정규화 벤더와 감지가 같으면 "변화 없음"으로
  오판해 DB를 갱신하지 않았음 → **저장된 원래 벤더와 비교**하도록 수정.
  이제 unknown 장비를 수집하면 벤더가 `cisco_ios` 등으로 채워짐(모델/버전 포함).
- 자동 수집이 저장 계정 없는 장비를 건너뛸 때 로그로 가시화.
- db 잠복 버그 수정: 일부 예외 경로의 미정의 log_event(NameError) 일괄 해결.

## v3.34.0 (2026-07-05) — 상태 필터 탭(오류/미수집) + 랙 뷰 구역 일괄 선택

- **현황판 상태 필터 탭**: `전체 / 정상 / 오류 / 미수집` 버튼(건수 표시)으로 리스트를
  걸러 보고, **그 리스트만 전체선택 → '정보 수집'으로 재수집**할 수 있습니다.
  - 오류 = 수집 실패 또는 도달 불가, 미수집 = 아직 한 번도 수집 안 됨.
  - 탭 전환 시 이전 선택은 자동 해제(다른 리스트 오수집 방지). 랙 뷰에도 필터 적용.
- **랙 뷰 구역별 일괄 선택**: 각 구역(📍 공장·건물·층) 제목의
  **"구역 전체 선택(N)"** 버튼으로 그 구역 스위치를 한 번에 선택 →
  상단 **'정보 수집(N)'** 으로 그 구역만 일괄 수집. 선택된 유닛은 파란 테두리,
  다시 누르면 해제.

## v3.33.2 (2026-07-05) — 수집 실패 사유 화면 표시

- **수집이 실패하면 그 원인을 화면에서 바로 확인**할 수 있습니다.
  - 스위치 현황 표: 상태 '실패' 아래에 **빨간 사유 문구**.
  - 현황판 카드: **⚠ 사유 요약**(마우스 올리면 전체).
  - 사유는 알기 쉬운 한국어로 분류: "장비 응답 없음(TCP-22) — 전원/케이블/방화벽 확인",
    "인증 실패 — 계정 확인", "수집 제한시간 초과", "OS와 명령/드라이버 불일치",
    "저장된 계정 없음", "SSH 협상 실패(구형 장비)" 등 + 원문 병기.
  - 다시 성공하면 사유는 자동으로 지워집니다.
  - (버그 수정: 이전엔 실패 사유가 로그에만 남고 DB/화면에 저장되지 않았음)

## v3.33.1 (2026-07-05) — '수집중' 고착 방지(하드 타임아웃) + 큐 정지 해소

- **수집이 '수집중' 상태로 고착되고 다음 스위치로 진행되지 않던 문제 수정.**
  - **장비당 하드 타임아웃(기본 480초)**: SSH 무한 대기 등 어떤 이유로든 제한시간을
    넘기면 그 장비만 강제 '수집 실패' 처리하고 **큐의 다음 장비를 즉시 진행**.
  - **타임아웃 연쇄 조기 중단**: 성공한 명령이 하나도 없는데 2연속 실패하면
    명령셋 불일치로 판정해 남은 명령의 타임아웃(수 분)을 낭비하지 않고 즉시
    폴백/실패 처리.
  - 결과: 문제 장비 1대가 전체 일괄/자동 수집을 세워두는 일이 없어짐.

## v3.33.0 (2026-07-05) — 스위치 현황에 모델명·OS 버전 표기

- **수집 시 `show version`에서 모델명과 OS 버전을 자동 추출**해 스위치 현황에
  **"모델 / 버전"** 컬럼으로 표기(모델 위, 버전 아래 2줄).
  - 예: `C9300-48P` / `IOS-XE 17.03.05`, `Nexus9000 C93180YC-EX` / `NX-OS 9.3(5)`,
    `X460G2-24t-10G4` / `EXOS 22.7.1.2`, Alteon 모델/버전 포함.
  - 별도 입력 없이 **수집 한 번이면 자동으로 채워집니다**(이미 수집된 장비는 재수집 시).
- 엑셀 보고서 '스위치 현황' 시트에도 모델/버전 컬럼 추가.

## v3.32.1 (2026-07-04) — EXOS 'pattern not detected' 근본 수정(원시 셸 OS 탐지)

- **EXOS 수집 시 `vendor_detect_failed: pattern not detected` 근본 원인 수정.**
  - 원인: EXOS 프롬프트는 명령마다 번호가 증가(`SW.1 #`→`SW.2 #`)하는데,
    벤더가 cisco로 등록돼 있으면 **cisco 드라이버**가 접속 시점 프롬프트를 고정
    기대해 `show version`부터 전부 매칭 실패 → 감지 자체가 불가능했음.
  - 수정: 드라이버 감지가 실패하면 **프롬프트 형식을 가정하지 않는 원시 셸
    (paramiko)로 show version을 실행해 OS를 탐지**한 뒤, **올바른 드라이버
    (extreme_exos 등)로 자동 재접속**해 수집. DB 벤더도 교정.
  - 드라이버 불일치로 판정되면 재시도 3회×명령 타임아웃(수 분)을 낭비하지 않고
    **즉시 폴백**(감지용 show version은 20초 제한).

## v3.32.0 (2026-07-04) — 토폴로지 가시성 전면 개선 (Meraki/UniFi 방식)

- **계층 트리 배치**: 백본(연결 최다, 🏢 강조) → 업링크 → 액세스 순 top-down 트리.
  자식이 부모 아래에 정렬되고 부모는 자식 중앙에 위치(선 교차 최소화).
- **정돈된 링크**: 세로 곡선(베지어)으로 연결. 실선=양방향 확인, 점선=보조/편방향.
- **호버 툴팁**: 링크에 마우스 → 양쪽 스위치·포트 정보, 노드에 마우스 → 장비 정보 +
  **연결된 링크 파란색 강조**. 포트 라벨 상시 표시 제거(어수선함 해소).
- **줌/팬**: 마우스 휠 확대/축소(커서 중심), 드래그로 이동 — 대규모 망도 탐색 가능.
- **미연결 장비 분리**: 링크 미발견 스위치는 지도에서 빼고 하단 칩 목록으로(클릭=상세).
- 노드 상태점(정상/실패·도달불가/경보) + 범례.

## v3.31.1 (2026-07-02) — 현황판 상단 공통 계정 + 선택 삭제

- **상단 공통 계정 입력.** 현황판 툴바에 **계정/비밀번호** 입력칸 추가 —
  여기에 입력해 두면 스위치 선택 후 **"정보 수집" 클릭 시 팝업 없이 즉시 수집**
  (대량 선택 시 팝업이 가려져 입력 불가하던 문제 해결). "저장" 체크 시 각 장비에
  암호화 저장(자동 수집 재사용). 비워두면 기존 팝업 방식 그대로.
  개별 스위치 수집 팝업에도 상단 계정이 자동 채워집니다.
- **현황판 선택 삭제.** 카드 체크박스로 선택한 스위치를 **"선택 삭제(N)"** 버튼으로
  일괄 삭제(관련 수집 데이터 포함).

## v3.31.0 (2026-07-02) — 항상 show version으로 OS 확인 후 그 OS 명령으로 수집

- **모든 수집에서 `show version`을 먼저 실행해 실제 OS를 확인**하고,
  그 OS의 명령셋으로 수집합니다(기존엔 벤더 '알 수 없음'일 때만).
  - **잘못 등록된 벤더 자동 교정**: EXOS 장비가 cisco로 등록돼 있어도
    (증상: `show interfaces status` command_failed) show version에서 ExtremeXOS를
    판별해 **EXOS 명령(show ports no-refresh/show fdb/show iparp)으로 전환**하고
    DB 벤더도 교정합니다. 다음 수집부터는 처음부터 올바른 접속 드라이버 사용.
  - 판별 실패 시엔 등록된 벤더 그대로 진행(동작 변화 없음).
- 인식 OS: Cisco IOS/IOS-XE · NX-OS · Arista EOS · ExtremeXOS · JUNOS.
  (Alteon은 메뉴형 CLI라 벤더를 alteon으로 직접 지정 — show version 경로 미적용)

## v3.30.2 (2026-07-02) — EXOS 실장비 형식 보강 + 실패 시 다음 장비 즉시 진행

- **ExtremeXOS 포트 상태 파싱 보강.** 실장비 `show ports no-refresh`는 상태가
  단어(active/ready)가 아니라 **한 글자 코드**로 나오는 형식이 흔함:
  Port State **E/D**(Enabled/Disabled) + Link State **A/R/NP**(Active/Ready/NotPresent).
  두 형식 모두 인식하도록 수정(A=up, R=down, D=disabled). `10G` 속도 표기도 인식.
  페이징 명령 오타 수정(`disable clipaging`).
- **수집 실패 시 다음 스위치로 즉시 진행.** 응답 없는 장비에 재시도 3회×타임아웃
  (~100초)을 낭비하지 않도록 **SSH 전 TCP-22 사전 확인** — 도달 불가면 즉시 실패
  처리하고 큐의 다음 장비를 진행(일괄/자동 수집 흐름이 빨라짐).
  ※ 큐 자체는 원래 장비별 격리라 한 대 실패가 나머지를 막지 않음 — 이번 수정은
  실패 판정을 100초 → 4초로 단축하는 것.

## v3.30.1 (2026-07-02) — config 일괄 다운로드(ZIP)

- 스위치 현황 탭에 **"config 일괄 다운로드"** 버튼 — 전체 스위치의 **최신 running-config
  백업을 ZIP 한 파일**로 다운로드(파일명: `이름_IP_날짜.txt`).
- 개별 다운로드는 기존처럼 스위치 상세 → "설정(config)" 탭에서.

## v3.30.0 (2026-07-02) — 관제(월보드) · 포트 이력 · 접근 로그

- **관제(월보드) 모드**: 상단 **"🖥 관제"** 버튼 → 새 탭 전체화면(`/wall`).
  대형 모니터용 다크 화면 — 큰 숫자 타일(도달불가/수집실패/경보/설비 연결실패/미확인
  알람), **문제 장비만** 큰 카드로(문제 없으면 "✓ 이상 없음"), 하단 최근 알람 티커,
  10초 자동 갱신, 읽기 전용. 문제 타일은 빨간 펄스로 강조.
- **포트 이력**: 스위치 상세에 **"포트 이력"** 탭 —
  (포트, MAC)별 **최초/최근 관측 시각 + 현재 연결 여부**. 언제 꽂혔고 뽑혔고
  교체됐는지 수집 이력으로 추적(추가 수집 없음 — 기존 스냅샷 재활용). 필터 지원.
- **접근 로그(감사)**: 상단 **📒** 버튼 — 어느 PC(IP)가 언제
  **수집/삭제/등록/설정 변경/보고서 다운로드**를 했는지 자동 기록·일괄 조회
  (최근 5000건 보관). 조회 폴링은 기록하지 않아 소음 없음.

## v3.29.0 (2026-07-02) — 토폴로지 맵 · 이메일 알람 · 설정 diff 뷰어

- **네트워크 토폴로지 탭 신설.** 수집된 ARP·MAC 테이블만으로 **스위치 간 연결 관계를
  자동 추론**해 계층형 SVG로 그립니다(장비 추가 접근 없음 — 부하 0).
  - 실선 = 양방향 확인 링크, 점선 = 한쪽만 관측, 링크에 **양쪽 포트 라벨** 표시.
  - 노드 색 = 상태(정상/실패/경보), 클릭하면 상세보기. 백본이 자동으로 최상단.
  - 포트채널(Po) 링크는 물리 멤버포트로 해석해 표기.
- **알람 이메일 발송.** 내부 SMTP 릴레이로 알람을 이메일 전송(데스크톱 알림/소리 없음).
  - "⏱ 자동 수집" 모달에서 설정: SMTP 서버/포트/발신/수신(여러 명 쉼표), 알림 수준
    (경고 이상만/전부), 인증 계정(선택, 암호화 저장), **테스트 발송** 버튼.
  - **60초 묶음 발송**으로 대역 스캔 중 수십 건 스팸 방지(1통에 요약).
- **설정(config) diff 뷰어.** 스위치 상세에 **"설정(config)" 탭** 추가 —
  백업 버전 목록에서 선택해 **직전 버전과 줄 단위 비교**(추가=초록/삭제=빨강),
  원문 다운로드. "설정 변경" 알람을 받았을 때 무엇이 바뀌었는지 바로 확인.

## v3.28.0 (2026-07-02) — 모니터링 스위트(도달성 감시·설비 이동·자동 스캔·config 백업)

- **① 스위치 도달성 감시**: PC에서 등록 스위치에 TCP-22 연결만 1분 주기로 확인(SSH 로그인
  없음 → **장비 부하 사실상 0**, 동시 10대 제한 + 분산으로 100대+ PC 부하도 미미).
  끊김/복구 전이 시 알람 + 현황판 카드에 **🔴 도달불가** 배지.
- **② 알람 보존 정책 + 필터**: 종류/기간/미확인 필터(알람 모달), 보존 기간(기본 90일,
  7~365) 지나면 자동 정리.
- **③ 설비 이동 감지**: 같은 MAC이 **다른 스위치/포트(직접 관측)** 로 옮겨지면
  "설비 이동" 알람(무단 이설 탐지). 어디서 어디로 옮겨졌는지 메시지에 표시.
- **④ 설비 대역 자동 스캔**: 설비 탭에서 한 번 수집한 대역을 기억해 **1일 1회 지정
  시각에 대역별 순차 스캔**(동시에 1개 대역만 — 부하 분산). 스위치에 저장된 계정 필요.
- **⑤ 포트 사용률**: 보고서 스위치 시트에 사용률(%) 컬럼 + 요약에 전체 포트 사용률
  (증설 계획 참고).
- **⑥ 설정(config) 백업 + 변경 알람**: 수집 때 running-config를 백업(변경 시에만 저장,
  스위치당 최근 20개). 이전과 달라지면 **"설정 변경" 알람**. 휘발성 라인(수집 시각 등)은
  제외해 오탐 방지. API로 백업 목록/원문 다운로드 가능.
- 설정은 "⏱ 자동 수집" 모달에서 한 번에 관리(자동 수집·설비 자동 스캔·도달성 감시·알람 보존).

## v3.27.0 (2026-07-02) — 보고서 재설계(필요한 정보만 한국어로)

- **보고서 내보내기(xlsx)를 전면 재설계.** 시트 5개, 전부 한국어·색상 표시:
  1. **요약** — 생성 시각, 스위치/방화벽/설비 통계, 미확인 알람 수,
     **확인 필요 장비 목록**(수집 실패·FLAP/LOOP 경보)을 첫 장에서 한눈에.
  2. **스위치 현황** — 구분/IP/호스트네임/벤더/**위치(TPS·서버실 해석)**/상태(색상)/경보/
     **포트(사용/전체)**/MAC 수/마지막 수집.
  3. **설비 현황** — 설비 탭과 동일(대역/IP/MAC/연결 스위치/포트/직접연결/상태 색상).
  4. **알람 이력** — 최근 500건(시각/종류/장비/내용/확인 여부).
  5. **포트 현황** — 스위치별 포트 상태/VLAN/속도/설명.
- 구버전의 '이벤트 로그'·'호스트 대장(장부 대조)' 시트는 기능 정리에 맞춰 제거.
- 머리행 고정(스크롤 시 헤더 유지), 한글 폭 반영 자동 열너비.

## v3.26.1 (2026-07-02) — 버그 검증(Opus) 후속 수정 + 탭 이름

- '스위치 리스트' 탭 → **'스위치 현황'** 으로 이름 변경.
- **Opus 코드 검증 결과 반영**(CRITICAL 1, WARNING 2 수정):
  - db.get_switches **중복 정의 제거**(첫 정의가 사장되던 잠재 위험).
  - 엑셀 업로드 SW/FW 구분을 **토큰 경계 기반**으로 강화 — `GBFW-SW01` 같은
    스위치가 방화벽으로 오분류되던 문제 수정(`FW01`/`DMZ-FW-01`은 방화벽 유지).
  - **오프라인 설비**는 "직접" 배지 대신 **"마지막 관측"**(회색)으로 표기 — 연결이
    끊긴 설비의 스위치/포트는 참고용(마지막 위치)임을 명확히. 엑셀/TXT 추출도 동일.

## v3.26.0 (2026-07-01) — 알람(변경 감지): 새 설비·연결 끊김·스위치 연결 실패

- **수집 결과를 이전 상태와 비교해 변경을 알람으로 표시**합니다(준실시간, 5초 폴링).
  - **새 설비 추가**: 대역 재수집 시 이전에 없던 IP/MAC 등장
  - **설비 연결 끊김**: 이전엔 온라인이던 설비가 이번 스캔에 사라짐 → 삭제하지 않고
    **"연결 실패"로 계속 표시** + 알람. 복구되면 온라인 복귀 알람.
  - **스위치 연결 실패/복구**: 수집이 실패(연결 불가)로 바뀌면 알람, 다시 성공하면 복구 알람.
  - **포트 flapping/looping**: 로그 이상 탐지 경보가 새로 발생하면 알람.
- 상단 **🔔 알람 벨 + 미확인 개수 배지** → 클릭 시 최근 이벤트 목록(시각·종류·장비),
  "모두 확인"으로 정리. 설비 현황의 오프라인 설비는 붉은 "연결 실패"로 강조.
- API: `GET /api/alerts`, `POST /api/alerts/ack`.

## v3.25.0 (2026-07-01) — Radware/Nortel Alteon(L4) 수집 지원

- **Alteon Application Switch(메뉴형 CLI) 수집 지원 추가.** netmiko가 지원하지 않는
  Alteon을 위해 **전용 paramiko 대화형 수집 경로**를 새로 만들었습니다.
  - 명령: `/info/l2/fdb/dump`(MAC), `/info/l3/arp/dump`(ARP), `/info/link`(포트), `/info/sys/general`(버전)
  - 페이징('more') 자동 처리 + 명령별 타임아웃(멈춤 방지).
  - 표준 Alteon OS 출력 형식 파서(MAC/ARP/포트) — 유연 매칭.
  - 연결 테스트도 Alteon은 paramiko 인증으로 확인.
  - 벤더 드롭다운에 "Radware Alteon (L4)" 추가. 등록 시 벤더를 **alteon(또는 radware)** 로 지정하세요.
  - ※ 실장비 출력 형식이 버전마다 다를 수 있어, 값이 비어 보이면 원본 출력을 공유해 주시면
    파서를 맞추겠습니다(수집은 원본을 저장하므로 진단 가능).

## v3.24.1 (2026-07-01) — ExtremeXOS 수집 실패 수정(실제 명령/형식)

- **ExtremeXOS 스위치 수집이 실패하던 문제 수정.** 원인은 `show ports`가 자동 갱신 화면이라
  응답이 끝나지 않아 멈춘 것 + MAC/ARP 명령이 EXOS 문법과 달랐던 것.
  - 포트 상태: `show ports no-refresh` (자동 갱신 방지) — 링크 상태 active/ready/disabled 인식.
  - MAC 테이블: `show fdb`, ARP: `show iparp`, 로그: `show log messages memory-buffer`.
  - 파서를 실제 EXOS 출력 형식(fdb/iparp/no-refresh)에 맞게 재작성.
- **수집 견고성 개선**: 한 명령이 미지원/타임아웃이어도 나머지는 수집하고, 원본 출력을
  저장하도록 명령별 개별 예외 처리(전부 실패 시에만 수집 실패 처리).
- EXOS 벤더 별칭 확대(extremexos/exos/extreme_xos 등 → extreme_exos).

## v3.24.0 (2026-07-01) — 벤더 자동 학습(수집 시 show version)

- **벤더가 '알 수 없음(unknown)'인 스위치를 수집하면 `show version`으로 실제 벤더를
  자동 학습**해 DB에 반영하고 현황판/스위치 리스트에 표시합니다.
  - 지원 인식: Cisco IOS/IOS-XE, Cisco Nexus(NX-OS), Arista(EOS), Extreme(EXOS), Juniper(JUNOS).
  - 학습된 벤더의 명령/파서로 이번 수집부터 맞춰 처리하고, 다음 수집부터는 처음부터 올바른
    장비 타입으로 접속합니다.
- 엑셀 가져오기로 vendor 없이 등록된 장비(또는 방화벽 unknown)도, 수집만 하면 벤더가 채워집니다.

## v3.23.1 (2026-07-01) — 엑셀 가져오기 장비 등록 수정 + 포트채널 포맷

- **엑셀 가져오기로 등록한 장비가 현황판/스위치 리스트에 안 나오던 문제 수정.**
  - 예전엔 **'vendor' 열이 있어야만 스위치**로 등록되고, 없으면 전부 '호스트(장부)'로
    분류돼 화면에 안 보였습니다. 이제 **장비 식별 컬럼(name/subnet/location 등)만 있어도
    스위치로 등록**하고, IP만 있는 목록도 등록합니다.
  - **hostname으로 스위치/방화벽 자동 구분**: hostname에 **`FW`가 있으면 방화벽**,
    아니면 스위치로 등록합니다(예: `DMZ-FW-01` → 방화벽, `..._SW1` → 스위치).
    방화벽은 벤더 미지정으로 등록되니 방화벽 현황에서 벤더/계정을 설정하세요.
  - 임포트 결과 안내에 방화벽 개수 추가.
- **NX-OS 포트채널 표기 `Po10`/`Po10(SU)` 모두 인식**(플래그 괄호 유무 무관).

## v3.23.0 (2026-07-01) — NX-OS 포트채널 멤버 해석(설비/TPS 백본 직결)

- **백본(NX-OS)에 포트채널(Po)로 직결된 TPS/설비의 실제 물리 포트가 확인되지 않던 문제 수정.**
  - NX-OS 수집 시 `show port-channel summary`를 함께 읽어 **Po → 물리 멤버포트**를 저장.
  - 설비 대조에서 MAC이 Po로만 보이면 **멤버 물리포트로 해석**해 직접 연결로 표시.
    예: 백본 Po10 → **"Eth1/1, Eth1/2 (Po10)"** + 연결 스위치=백본, 직접 연결로 인식.
- 적용: **백본 NX-OS 스위치를 이 버전으로 다시 일반 수집**한 뒤, 설비 현황에서
  **"새로고침(재매칭)"** 을 누르면 즉시 반영됩니다.

## v3.22.0 (2026-07-01) — 현황판 위치 통합 · UI 정리 · 검색 강화 · 설비 추출

- **현황판 랙 뷰에서 백본 스위치·방화벽 위치가 "위치 미상"으로 나오던 문제 수정.**
  TPS 호스트네임이 아니어도 **위치(location)** 로 그룹핑(서버실 랙 A09U27 / 자유 위치 텍스트).
  방화벽도 현황판(카드·랙 뷰)에 함께 표시됩니다(🛡).
- **서버실 현황 카드 뷰에서 방화벽과 스위치 표현 통일.** 방화벽 카드를 스위치 카드와 동일한
  골격으로 렌더링(현황판·서버실 공용).
- **'스위치 현황' 탭 → '스위치 리스트'** 로 이름 변경.
- **'장부 대조' 탭 제거**(사용자 요청).
- **상단 검색 강화:** HOSTNAME·IP·MAC 어떤 값으로도 검색. 특히 **MAC은 형식 무관**
  (`0050.56a1.b2c3` / `00:50:...` / `0050-56a1-...` / 무구분 모두 매칭). 방화벽은 위치로도 검색.
- **설비 현황 전체를 엑셀(xlsx)·TXT로 추출** 버튼 추가(TXT는 엑셀에서 한글 정상 표시되는 UTF-8 BOM).

## v3.21.0 (2026-07-01) — 설비 백본 직접연결 인식 + 방화벽 인터페이스 IP/위치

- **백본(NX-OS) 등에 직접 연결된 설비가 "직접 연결 미확인"으로 나오던 문제 수정.**
  물리 포트로 관측된 위치가 하나뿐이면(다른 스위치는 Po/Vl 업링크로만 보임) 그 스위치를
  직접 연결로 인식합니다. 포트가 MAC을 많이 학습하는 백본/서버 포트도 정상 인식.
- **방화벽 인터페이스 IP가 실제와 다르게 나오던 문제 수정.**
  - FortiGate: 설정값(cmdb) 대신 **실제 런타임 IP(monitor 엔드포인트)** 우선 사용
    (DHCP/PPPoE 할당 IP 반영), 실패 시 cmdb로 폴백.
  - Palo Alto: `show interface logical`에서 **실제 인터페이스 IP** 파싱(기존엔 IP 비어 있었음).
- **방화벽 등록에 위치(location) 추가.** `A09U27` 형식으로 넣으면 **서버실 현황**에
  스위치와 함께 랙/카드 뷰로 표시됩니다(🛡 아이콘). 방화벽 목록에도 위치 열 표시.

## v3.20.2 (2026-07-01) — 구형 장비 SSH 접속 오류(incompatible ssh peer) 수정

- 계정이 정상인데도 **"연결 실패 [auth] ... incompatible ssh peer"** 로 접속이 안 되던 문제
  수정. 원인은 최신 paramiko(5.0)가 구형 스위치의 SSH 알고리즘(SHA1 기반 KEX,
  ssh-rsa 호스트키, aes-cbc)을 **구현에서 제거**해 협상이 실패한 것.
  - 레거시 구현이 남은 **paramiko 3.5.1로 고정**.
  - `core/ssh_compat.py`가 SHA1 KEX·ssh-rsa·aes-cbc 등을 **폴백 선호목록에 재추가**
    (강한 알고리즘이 여전히 우선 협상 — 보안 저하 최소화).
- 연결 테스트 오류 메시지를 알기 쉬운 한국어 안내로 개선(협상 실패/인증 실패/타임아웃 구분).

## v3.20.1 (2026-07-01) — 설비 현황 가독성 개선 + 새로고침(재매칭)

- **표를 직접 연결 중심으로 정리.** 이 페이지의 목적(설비가 어느 스위치에 **직접** 연결되어
  있는지)에 맞춰, 업링크 경유(Po·Vl 등) 상세는 표에서 빼고 **툴팁**으로만 보여줍니다.
  - 직접 연결: **연결 스위치 + 물리 포트(Gi/Te)** + "직접" 배지
  - 미확인: **"직접 연결 미확인 ⓘ"** (마우스 올리면 어디서 업링크로 관측됐는지 표시), 포트는 "—"
  - 상단에 **전체/직접/미확인 건수 요약** + **"직접 연결만"** 보기 토글.
- **"↻ 새로고침(재매칭)" 버튼 추가.** ping 없이 **최신 MAC 테이블 기준으로 연결 스위치/포트를
  재대조**합니다(빠름). TPS 스위치를 다시 일반 수집한 뒤 이 버튼만 누르면 즉시 최신화됩니다.
  (새 IP 탐지·온오프 갱신은 기존 "대역 수집(ping)"으로.)
- 백엔드 `POST /api/facility/rematch`.

## v3.20.0 (2026-07-01) — 공통 계정 일괄 정보 수집

- **현황판에서 여러 스위치를 한 번의 계정 입력으로 동시에 수집.** 공통 계정을 쓰는
  장비를 한꺼번에 수집할 때 유용합니다.
- 현황판 카드마다 **선택 체크박스** + 상단 **전체선택** + **"정보 수집(N)"** 버튼.
- 선택 후 "정보 수집" → **팝업에서 공통 아이디/패스워드 입력** → **"수집 시작"** 하면
  선택 장비를 백그라운드로 **동시 수집**(기본 3대씩 병렬, 순차 처리로 전부 완료).
- 계정 저장 체크 시 각 장비에 암호화 저장(자동 수집에 재사용).
- 선택 상태는 화면 자동 새로고침(5초)에도 유지됩니다.
- 보안: 각 장비 IP를 수집 직전 재검증(SSRF 방어), 계정은 세션 저장소 경유(평문 큐 비노출).
- 백엔드 `POST /api/switches/bulk-collect`.

## v3.19.0 (2026-07-01) — 서버실 현황 탭 · VLAN 그룹 뷰 · 스위치 일괄삭제 · 컬럼명

- **서버실 현황 탭 신설.** 스위치 위치(location)를 `A09U27` 형식으로 기재하면
  **A09 랙 U27**에 배치된 것으로 인식해, 서버실 장비만 따로 **카드 뷰 / 랙 뷰**로 보여줍니다.
  랙 뷰는 랙별로 유닛(U번호) 순서로 쌓아 실제 랙처럼 표시합니다.
- **VLAN 탭을 VLAN 기준 그룹으로 개편.** VLAN 번호를 클릭(드롭다운)하면 그 VLAN이 있는
  **스위치 목록(구분·호스트네임·IP·MAC 수)** 을 펼쳐 봅니다. VLAN당 스위치 수·총 MAC 수 요약,
  VLAN 번호/이름 검색 지원.
- **스위치 현황 상세에 체크박스 추가 → 선택/전체 일괄 삭제.** 상단 "선택 삭제(N)" 버튼으로
  여러 대를 한 번에 삭제(관련 수집 데이터도 함께 정리).
- **스위치 현황 상세의 '이름' 컬럼명을 '구분'으로 변경.**

## v3.18.4 (2026-06-30) — 상단 IP 검색 결과 표 잘림 수정

- 검색 결과 모달이 400px 고정폭이라 표(구분·IP·이름·상세)의 긴 내용(MAC·포트·대역)이
  잘려 보이던 문제 수정. 검색 모달을 넓게(최대 화면폭) + 세로 스크롤 + 셀 줄바꿈 허용.

## v3.18.3 (2026-06-30) — 설비 직접연결 스위치 판별 정확도 개선

실장비 피드백 반영. 이전에는 설비 MAC이 여러 스위치에 보이면 **Po(포트채널)·Vl(VLAN/SVI)
같은 업링크 경유 포트까지 "연결 스위치"로 잘못 표시**했습니다. 이제 진짜 직접 연결을 가려냅니다.

- **논리 포트(Po·Port-channel·Vl·Vlan·Lo·Tu·ae 등)는 직접 연결에서 제외.** 그 MAC은
  업링크/트렁크를 타고 넘어온 것이므로 설비가 직접 붙은 곳이 아닙니다.
- **물리 포트(Gi·Te·Fa·Eth 등) 중 "해당 포트의 MAC 수가 가장 적은 포트 = 액세스 포트"**
  를 직접 연결로 선택합니다. 트렁크 포트는 MAC이 많아 자동 배제됩니다.
- 각 설비에 **직접 / 간접·업링크 배지**를 표시. 직접 연결을 확신할 수 없으면(논리 포트뿐이거나
  트렁크만 관측) "간접·업링크"로 정직하게 표기하고, 그 외 관측 위치를 함께 보여줍니다.
- (정확도를 높이려면: 대역의 **각 TPS 스위치를 일반 수집**해 최신 MAC 테이블을 채운 뒤
  11번에서 대역 수집을 하세요. 액세스 스위치가 미수집이면 트렁크로만 보여 "간접"으로 잡힙니다.)
- DB: facility_hosts에 `direct`·`via` 컬럼 추가(기존 DB 자동 마이그레이션).

## v3.18.2 (2026-06-30) — 상단 검색 보강(설비·MAC) + 설비 수집 크래시 수정

- **상단 검색이 설비 현황·MAC 테이블을 못 찾던 문제 수정.** 이전에는 등록
  스위치/방화벽·ARP(IP만)·장부만 검색해, 수집된 **설비 IP**나 **MAC**으로
  검색하면 0건이 나왔습니다. 이제 다음을 모두 검색합니다:
  - 설비 현황(facility_hosts) — **IP·MAC** 모두
  - 스위치 MAC 테이블(최신 스냅샷) — **MAC**으로 어느 스위치·포트인지
  - ARP(스위치·방화벽) — **IP뿐 아니라 MAC**으로도
  - 기존 등록 스위치/방화벽·장부 호스트(IP·이름)
- 검색창 안내문구를 "IP·이름·MAC 검색"으로 갱신.
- **설비 대역 수집 완료 시 크래시 수정**: 완료 직전 `len(hosts)`가 미정의 변수라
  `show ip arp` 후 저장 단계에서 예외가 발생하던 버그(NameError)를 바로잡음.

## v3.18.1 (2026-06-30) — 설비 대역 자동 도출 + 검증 후속 수정

- 설비 현황 탭에 **"대역 자동 찾기"** 버튼 추가. 선택한 11번 스위치에서
  `show ip route connected` / `show ip interface`를 읽어 **directly-connected 대역을
  자동 도출**합니다(여러 대역 분포도 모두 인식).
- 도출된 대역이 입력칸에 채워지고, 여러 개면 목록을 보여줘 바꿔가며 수집할 수 있습니다.
- (수동 CIDR 입력도 그대로 가능. /22 이하만 인식해 과대 대역은 제외.)
- **검증 후속 수정**(Opus 헌팅):
  - 동시 수집 요청 시 두 작업이 겹치던 경합 수정(시작 즉시 잠금).
  - 한 IP가 **여러 스위치 포트**에 보일 때 마지막만 남던 손실을 **한 행에 합쳐 보존**.
  - 재수집 시 해당 대역 기존 결과를 비워 중복 누적 방지.

## v3.18.0 (2026-06-30) — 설비 현황판 (대역 ping + ARP·MAC 대조)

- **설비 현황 탭** 추가(2번째). 각 대역의 **11번 TPS 스위치가 직접 대역 전체에 ping**해
  ARP를 학습하고, 그 IP/MAC을 **등록된 모든 스위치의 MAC 테이블과 대조**해 어느 스위치
  어느 포트에 연결됐는지 찾아 리스트업합니다.
- 사용법: 11번 스위치 선택 + 대역(CIDR, 예 `10.92.174.0/23`) 입력 → "대역 수집(ping)".
  백그라운드로 진행되며 진행률이 표시됩니다(스위치 계정은 저장된 자격증명 사용).
- 대역 크기는 `/22` 이하로 제한(ping 폭증 방지). 결과는 IP/MAC/스위치/포트/온라인으로 표시.
- (참고: ping은 PC가 아니라 11번 스위치가 직접 수행 — PC→설비는 방화벽에 막히므로.)

## v3.17.0 (2026-06-30) — 장비 메모 + 이벤트 로그 탭 정리

- **메모 기능**: 스위치에 메모를 달 수 있습니다(점검 이력·담당자 등). 추가/수정 모달의
  "메모" 칸에 입력하면 현황판 카드에 📝로 표시됩니다.
- **이벤트 로그 탭 제거**: 시스템 로그 탭과 중복되어 상세 화면에서 제거했습니다.
- (부수 수정) 일괄 등록 시 subnet/note가 저장되지 않던 부분도 함께 수정.

## v3.16.2 (2026-06-30) — enable 모드 진입 (user 모드 장비 수집)

- 증상: 로그인 후 `호스트명>`(user 모드)인 장비에서 모든 명령이 `% Invalid input`.
- 원인: IOS-XE는 user 모드에서 show 명령을 거부 → **enable(특권) 모드 진입 필요**.
- 수정: 수집 시 user 모드면 **자동으로 enable 진입**(로그인 비밀번호를 enable 비밀번호로
  1차 시도). 이미 특권 모드(`#`)면 그대로 진행.
- (enable 비밀번호가 로그인과 다른 환경은 다음 버전에서 별도 입력 필드 제공 예정.)

## v3.16.1 (2026-06-30) — 버그 검증 후속 수정

Opus 2종 버그 헌팅 결과:
- **(중요) 포트 파싱 오염 수정**: 포트 이름에 `SRV-DOWN-LINK`처럼 up/down/connected
  단어가 있으면 상태를 잘못된 위치에서 분리해 **상태·VLAN·속도가 오염**되던 버그를
  수정(정확 토큰 일치로 판별). 회귀 테스트 추가.
- **일괄 등록 zip-bomb 방어**: `import-inventory`에도 압축폭탄 검증 추가(다른 업로드와 일관).
- **랙 뷰 필터 즉시 반영**: 랙 뷰에서 위치 필터 입력 시 바로 갱신.

## v3.16.0 (2026-06-30) — 위치별 서버실 랙 뷰

- 현황판에 **"랙 뷰"** 토글 추가(카드 뷰 ↔ 랙 뷰).
- TPS 위치(hostname 파싱)로 스위치를 **공장·건물·층 → TPS(랙) → 스위치(유닛)**로
  그룹핑해 **서버실 랙처럼** 표시합니다. 유닛 색으로 상태 구분, 클릭 시 상세.
- 위치 미해석 스위치는 "위치 미상" 그룹으로 모읍니다. 위치 필터도 적용됩니다.

## v3.15.0 (2026-06-30) — 벤더 확장(로그 탐지 전벤더 + Arista CRC/errors)

- **시스템 로그 이상 탐지를 전 벤더 적용**: Cisco IOS/IOS-XE·Arista(`show logging`),
  Extreme(`show log`), NX-OS 모두 수집·탐지(플래핑/루프/오류). 범용 분석기라 Extreme
  "Port x:y link down/up" 같은 형식도 인식합니다.
- **Arista CRC/입출력 오류** 병합(`show interfaces` 상세, 약어 통일로 정확 매칭).
- (NX-OS errors·벤더별 포트 상태 세분화는 실장비 출력 확인 후 보강 예정.)

## v3.14.0 (2026-06-30) — 포트 CRC/입출력 오류 모니터링

- 수집 시 `show interfaces`(전체 상세) 1회로 **모든 포트의 CRC·input/output errors**를
  파싱해 상세 포트 탭에 표시합니다(0 초과 시 빨간색 강조).
- 포트별 개별 명령 없이 한 번에 수집(효율적). 포트 목록은 기존 status/description 기준.
- (Cisco IOS/IOS-XE 적용. Arista/Extreme/NX-OS 동일 동작은 후속 보강 예정.)

## v3.13.0 (2026-06-30) — 시스템 로그 수집 + 이상 탐지

- 수집 시 **show logging**(Cisco/Arista) / **show log**(Extreme)를 함께 읽어 상세 화면의
  **"시스템 로그"** 탭에 **최근 15줄**을 표시합니다.
- 로그에서 **이상 자동 탐지**:
  - **플래핑**: 같은 포트 link up/down 반복(임계 3회+)
  - **루프**: MAC flapping(포트 간 이동)·loop-guard·loopback·ELRP 등
  - **오류**: err-disable 등
- 탐지 시 유형별로 표시하고 경보 색으로 강조합니다.

## v3.12.0 (2026-06-30) — 포트 상태 세분화 + 속도/듀플렉스/타입

- **포트 상태 구분**: `connected`(up) 외에 **notconnect / disabled / err-disabled**를
  구분해 표시합니다(과거엔 모두 down으로 합쳐짐). 배지 색도 구분(up=초록, err-disabled=빨강,
  나머지=회색).
- **속도 누락 수정**: `show interface status`의 **Duplex·Speed·Type**(예: a-full · a-1000 ·
  10/100/1000BaseTX, auto-duplex/auto-speed)을 함께 파싱해 속도 칸에 표시합니다.

## v3.11.0 (2026-06-30) — 장비 일괄 등록 (IP/SUBNET/HOSTNAME 엑셀)

- 현황판의 **"장비 일괄등록(IP목록)"** 버튼으로 IP·서브넷·HOSTNAME 엑셀을 업로드하면
  **스위치로 일괄 등록**됩니다(벤더는 "알 수 없음"으로 들어가고 이후 수정/수집 시 지정).
- 컬럼명은 유연 인식: IP(`ip`/`address`), 서브넷(`subnet`/`ip subnet`/`대역`),
  HOSTNAME(`hostname`/`사용서버명`).
- 허용 대역 밖 IP(SSRF)는 자동 제외하고 건수를 안내합니다.
- 등록된 hostname이 TPS 포맷이면 위치도 자동 표시됩니다(v3.10.0).

## v3.10.0 (2026-06-30) — 하루 2회 자동 수집 + 위치 필터

**자동 수집 (하루 N회)**
- 현황판의 **"⏱ 자동 수집"** 버튼에서 on/off + 시각(예: `06:00,18:00`)을 설정합니다.
- 설정 시각에 저장된 자격증명이 있는 스위치/방화벽을 자동 일괄 수집합니다.
- 스위치는 수집 시 **"계정 저장"** 체크 시에만 자동 수집 대상(암호화 저장, 화면 비노출).
- 문제 발생 시 **수동 수집은 그대로** 가능합니다.

**위치 필터**
- 현황판·스위치 현황 상단의 **"위치로 필터"**에 입력하면 해당 위치 장비만 표시됩니다.

**TPS 위치 자동 해석**
- hostname이 `...F1B02_1F01...` 형식이면 **공장/건물/층/TPS**를 자동 해석해 현황판
  카드·스위치 목록·상세에 표시합니다(예: "1공장 Assembly(B02) 1층 TPS01").
- 접두(TPS-/SKBA_ 등)는 무관하게 `F{공장}B{건물}_{층}F{TPS}` 패턴을 인식합니다.
- 위치 필터는 위치·TPS 라벨(건물명 등)·hostname을 모두 검색합니다.

**보안 수정**:
- 스위치 목록 조회가 자격증명(cred_blob)을 노출하던 부분을 차단.
- 자동 수집 경로도 수동과 동일하게 대상 IP를 SSRF 재검증(허용 대역 밖/예약 IP 차단).

## v3.9.0 (2026-06-30) — 상세 정보 화면 개선(넓은 중앙 모달 + 요약 통계)

- 장비 상세보기를 **넓은 중앙 모달**로 변경해 정보를 한눈에 보기 쉽게 했습니다(기존
  좁은 우측 슬라이드 패널 → 화면 중앙 큰 창).
- 상단에 **요약 통계** 추가: 전체 포트 / Up / Down / MAC / ARP / VLAN 개수.
- 각 탭(포트/MAC/ARP)의 검색창은 그대로 유지됩니다.

## v3.8.1 (2026-06-30) — 종합 검색 + 현황판 삭제 + IOS 명령 보정

- **최상단 검색 개선**: 기존엔 엑셀 장부만 검색했으나, 이제 **등록 스위치/방화벽 IP·이름,
  수집된 스위치/방화벽 ARP, 장부 호스트**를 모두 종합 검색합니다(부분 일치 지원).
- **현황판 카드 삭제**: 대시보드 현황판 카드에서도 장비를 삭제할 수 있습니다.
- **Cisco IOS 수집 보정**: IOS 호환을 위해 복수형 명령(`show interfaces status` /
  `show interfaces description`)으로 변경(IOS/IOS-XE 공통).
- **벤더 "알 수 없음" 수집**: 벤더 미지정 시 Cisco IOS 명령으로 시도(fallback).

## v3.8.0 (2026-06-30) — VLAN 이름 표시

- VLAN 현황에 **VLAN 이름(Name)** 컬럼을 추가했습니다.
- 수집 시 `show vlan brief`를 읽어 VLAN ID·이름·상태를 저장하고, VLAN 목록에
  이름과 함께 표시합니다(MAC이 없는 VLAN도 이름과 함께 노출).
- Cisco IOS/IOS-XE/Nexus 공통 지원.

## v3.7.2 (2026-06-30) — 상세보기 버그 수정 + 검색 기능

- **상세보기 버그 수정**: 스위치 카드의 "상세보기"를 누르면 수집(연결 테스트) 팝업이
  같이 뜨던 문제를 수정 — 이제 상세 정보만 표시됩니다.
- **상세 정보 검색**: 스위치 상세의 포트/MAC/ARP 목록에 검색창을 추가했습니다.
- **방화벽 ARP 검색**: 방화벽 상세의 ARP 목록에도 검색창을 추가했습니다.
- (포함) v3.7.1의 Cisco IOS/IOS-XE 수집 개선 + dot MAC 파싱 버그 수정.

## v3.7.1 (2026-06-30) — Cisco IOS/IOS-XE 수집 개선 + MAC 파싱 버그 수정

- **MAC 파싱 버그 수정(중요)**: 기존 IOS 파서가 colon 형식 MAC만 인식하고 Cisco의
  dot 형식(`0050.56a1.b2c3`)을 놓쳐, mac 출력이 있어도 0건으로 저장되던 버그를
  수정했습니다.
- **명령 세트를 IOS-XE/Catalyst 표준으로 통일**: `show interface status` /
  `show interface description` / `show mac address-table` / `show ip arp`. 포트
  상태·VLAN·속도·설명을 안정적으로 수집합니다(`show interface status` 테이블 파싱).
- IOS-XE/Catalyst 혼재 환경 호환성이 향상됩니다. (Nexus 장비는 "Cisco Nexus(NX-OS)"
  벤더로 등록하세요.)

## v3.7.0 (2026-06-30) — Cisco Nexus(NX-OS) 지원 추가

- 증상: Cisco Nexus 스위치 수집 시 MAC만 수집되고 포트/ARP는 `% Invalid command at
  '^' marker.`로 빈 결과(수집은 "정상"으로 표시).
- 원인: NX-OS는 IOS와 명령이 다른데(IOS용 `show interfaces`/`show arp` 사용) NX-OS
  벤더가 없어 IOS 명령을 보냈습니다.
- 수정: **Cisco Nexus(NX-OS) 벤더 추가** — 올바른 명령(`show interface brief` /
  `show interface description` / `show ip arp`)과 NX-OS 전용 파서(dot 형식 MAC 등).
- 스위치 수동 추가 벤더 목록에 **"Cisco Nexus (NX-OS)"**가 추가됐습니다.

## v3.6.1 (2026-06-30) — 자격증명 복호화 버그 수정

- 증상: 저장된 API 토큰으로 수집 시 "token 또는 username/password 중 하나 필요" 오류.
- 원인: DPAPI 복호화에서 `CryptUnprotectData` 반환값의 잘못된 인덱스(`[0]` 빈 설명)를
  사용해 복호화가 항상 실패했습니다(저장된 토큰이 빈 값으로 전달됨).
- 수정: 데이터 인덱스(`[1]`)를 사용하도록 고쳐 저장된 토큰으로 정상 수집됩니다.

## v3.6.0 (2026-06-30) — FortiGate ARP 404 수정 + 자격증명 재사용

**FortiGate ARP 수집 404 수정**
- 증상: 방화벽 수집 시 `404 ... /api/v2/monitor/router/arp` 오류.
- 원인: FortiOS 버전에 따라 ARP REST 경로가 다른데(7.x는 `network/arp`), 한 경로만
  사용해 해당 장비에서 404가 났습니다.
- 수정: ARP 경로를 **버전별로 순서 시도**(`network/arp` → `router/arp`)하고, 어떤
  경로도 없으면 예외 없이 빈 결과로 처리해 수집 전체가 실패하지 않게 했습니다.

**자격증명 재사용 (매번 토큰 입력 불필요)**
- 방화벽 등록 또는 첫 수집 때 입력한 API 토큰/계정을 **암호화 저장**합니다.
- 이후 수집 버튼을 누르면 **저장된 자격증명으로 바로 수집**(입력창 생략)합니다.
- FortiGate는 **API 토큰만으로 통신**됩니다 — 토큰만 넣으면 아이디/비밀번호 불필요.
- (저장 자격증명은 암호화되며 화면·API로 노출되지 않습니다)

## v3.5.3 (2026-06-30) — 연결 테스트 출발지 IP 표시/안내

- 연결 테스트 결과에 **실제 사용한 출발지(접근) IP**를 함께 표시합니다.
- 헤더 "접근 IP"가 **"자동(OS 기본)"**이면, OS가 임의 IP(127.0.0.1 등)를 출발지로
  고를 수 있다는 안내를 띄워 이더넷 IP를 선택하도록 유도합니다.
- (연결 테스트는 기존에도 설정한 접근 IP로 바인딩했으나 화면에 보이지 않아 확인이
  어려웠던 점을 개선)

## v3.5.2 (2026-06-29) — 버그 검증 후속 수정

전체 코드 버그 검증(Opus 2종 + 199 테스트) 결과 발견한 항목 수정:
- **UI 레이아웃/색상 깨짐 수정**: CSP가 inline `style`까지 차단하던 것을 `style-src
  'unsafe-inline'`로 허용(스크립트는 'self' 유지 → 보안 동일).
- **수정 시 중복 오류 메시지 개선**: 이름/IP(스위치)·호스트(방화벽)가 이미 존재하면
  불명확한 500 대신 "이미 사용 중" 안내(409).
- **Extreme(EXOS) 수집 개선**: 페이징 비활성화에 `terminal length 0`(Cisco용) 대신
  벤더별 명령(`disable clpaging`)을 사용하고, 실패해도 수집을 계속합니다.

## v3.5.1 (2026-06-29) — 수집 read_timeout 오류 + 수정/삭제 버튼 무반응 수정

- **수집 `read_timeout` 오류 수정**: `read_timeout`은 netmiko `send_command()`의 인자인데
  `ConnectHandler` 생성자에 잘못 전달해 `unexpected keyword argument 'read_timeout'`
  오류가 났습니다. send_command로 옮겼습니다.
- **수정/삭제 버튼 무반응 수정**: 보안 정책(CSP)이 inline onclick을 차단해 동적 버튼
  (수정·삭제·상세·수집)이 동작하지 않았습니다. 이벤트 위임 방식으로 전환해 CSP를
  유지하면서 정상 동작합니다.

## v3.5.0 (2026-06-29) — 수집 벤더 오류 수정 + 수정 기능

**스위치 수집 'unsupported device_type' 오류 수정**
- 증상: 연결 테스트는 성공하나 정보 수집 시 `unsupported 'device_type'` 오류.
- 원인: 수집 경로가 벤더 값(`cisco`)을 netmiko 타입으로 그대로 사용(연결 테스트는 매핑함).
- 수정: 수집 경로도 `cisco→cisco_ios` 등으로 정규화(device_type·명령·파서 일관).

**스위치 / 방화벽 수정 기능 추가**
- 스위치 현황 표·방화벽 목록의 **"수정"** 버튼으로 등록 정보(이름/IP/벤더/포트 등)를 변경.
  (방화벽 수정 시 자격증명은 유지되며, 변경하려면 재등록)
- 삭제 기능(기존)과 함께 잘못 등록한 장비를 정리할 수 있습니다.

## v3.4.2 (2026-06-29) — 수집 재시도/출발지 바인딩 안정화

- 스위치 수집(SSH)의 재시도 로직이 2회차부터 내부 설정이 비워져 깨지던 버그를 수정했습니다.
  이제 재시도 시에도 출발지 IP 바인딩(접근 IP 설정)이 일관되게 적용됩니다.
- 1회차 연결 실패(일시 장애 등) 후 재시도가 정상 동작합니다.

## v3.4.1 (2026-06-29) — 핫픽스 (netmiko 번들 누락)

- 증상: 스위치 연결 테스트/수집 시 `No module named 'netmiko'`.
- 원인: SSH 라이브러리(netmiko)가 함수 내부 lazy import라 PyInstaller가 일부만 번들,
  데이터 파일(ntc-templates)·동적 모듈이 누락됨.
- 수정: 빌드 설정에서 `collect_all`로 netmiko/paramiko **전체(모듈+데이터+바이너리)**를
  번들 → 스위치/Palo Alto SSH 및 연결 테스트가 정상 동작.

## v3.4.0 (2026-06-29) — M12 출발지 IP 바인딩 (장비 ACL 통과)

**장비 접근 출발지(source) IP 선택**
- PC에 NIC가 여러 개(물리/VPN/가상)일 때, 장비로 나가는 출발지 IP가 장비 ACL 허용 IP와
  달라 차단되던 문제를 해결합니다.
- 헤더의 **"접근 IP"** 드롭다운에서 출발지로 쓸 PC 이더넷 IP를 선택하면, 이후 모든 장비
  접근(스위치 SSH, FortiGate REST, Palo Alto SSH, 연결 테스트)이 그 IP로 바인딩됩니다.
- "자동(기본 라우팅)" 선택 시 기존처럼 OS가 결정합니다.
- 선택값은 PC의 실제 이더넷 IP 목록으로 제한(오설정 방지).

## v3.3.2 (2026-06-29) — 방화벽 삭제 오류 수정

- 방화벽 삭제 시 발생하던 operational error를 수정했습니다. 관련 테이블
  (firewall_interfaces/firewall_arp)이 구버전 DB에 없어도 각 삭제를 개별 보호해
  방화벽이 정상 삭제됩니다.

## v3.3.1 (2026-06-29) — 스위치 등록 오류 수정 + 삭제 기능

**스위치 수동 추가 DB 오류 수정**
- 증상: 스위치 등록 시 `DB_ERROR` / `ADD_SWITCH_MANUAL_ERROR` (구버전 스키마 DB).
- 원인: 등록 로직이 구버전 `switches`에 없는 컬럼(hostname/vendor/location/status/alert)을
  참조해 "no such column"으로 실패.
- 수정: 실제 존재하는 컬럼만 골라 동적으로 INSERT/UPDATE → 어떤 스키마의 DB에서도 안전.

**삭제 기능 추가**
- 스위치 현황 표·방화벽 목록에 **삭제** 버튼 추가(잘못 등록 시 제거).
- 스위치 삭제 시 관련 수집 데이터(스냅샷/포트/MAC/ARP/이벤트)도 정리(호스트 인벤토리는 보존, 위치만 무효화).
- 방화벽 삭제 시 인터페이스/ARP도 함께 제거.

## v3.3.0 (2026-06-29) — M11 장비 연결성 / 등록 개선

**연결 테스트 (수집 전 선검증)**
- 스위치/방화벽 수집 모달에 "연결 테스트" 버튼 추가. IP+계정 입력 후 즉시 접근 가능
  여부를 확인(TCP 도달성 → 인증). 결과를 색상으로 표시.

**방화벽 API Key/자격증명 저장**
- 방화벽 추가 시 API 토큰/계정을 입력하면 **DPAPI로 암호화하여 저장**, 이후 수집 때
  재입력이 필요 없습니다. (자격증명 blob은 어떤 조회 API에도 노출되지 않음)
- 수집 결과(done/failed)가 목록에 상태로 표시됩니다.

**PC 이더넷 IP 표시**
- 헤더에 PC의 이더넷 IP를 표시합니다. 장비 접근에 사용되는 실제 IP를 안내하며,
  127.0.0.1(루프백)로는 장비에 접근할 수 없음을 알려줍니다.

**보안**: 연결 테스트/등록/수집 모두 host 허용대역·port(1-65535) 검증(SSRF 방어). 저장된
자격증명 blob은 GET 응답·UI에 노출하지 않음.

## v3.2.2 (2026-06-29) — 핫픽스 (ON CONFLICT 전면 제거)

v3.2.1은 스위치 수동 추가만 고쳤으나, 동일 원인이 **엑셀 업로드(호스트/장부 적재)와
방화벽 등록**에도 있었습니다. 구버전 스키마 DB에서 해당 UNIQUE 제약이 없으면 같은
`ON CONFLICT` 오류가 났습니다.

- 수정: `save_hosts`, `save_ledger_hosts`, `save_firewall`의 `ON CONFLICT`를 모두
  제거하고 수동 UPSERT(SELECT→UPDATE/INSERT)로 전환 — 어떤 스키마의 DB에서도 안전.
- 기존 보존 로직(측정/장부 컬럼 분리, 빈값 시 기존값 유지)은 그대로 유지.

## v3.2.1 (2026-06-29) — 핫픽스 (수동 추가 Internal server error)

**구버전 DB에서 스위치 수동 추가 실패 수정**
- 증상: 스위치 수동 추가 시 `Internal server error` —
  "ON CONFLICT clause does not match any primary key or unique constraint".
- 원인: 구버전 스키마로 생성된 DB의 `switches` 테이블에 `name` UNIQUE 제약이 없는데,
  `import_switches_bulk`가 `ON CONFLICT(name)`을 사용.
- 수정: `ON CONFLICT` 의존을 제거하고 name 기준 **수동 UPSERT(SELECT→UPDATE/INSERT)**로
  변경 — UNIQUE 제약 유무와 무관하게 모든 DB에서 안전하게 동작(멱등 유지).

## v3.2.0 (2026-06-29) — M10 방화벽 현황 (Palo Alto / Fortinet)

**방화벽 모니터링 기능 추가**
- 방화벽 장비(Fortinet FortiGate, Palo Alto PAN-OS)를 등록하고 **인터페이스 + ARP
  (연결된 IP/MAC)** 를 수집해 "방화벽 현황" 탭에 표시합니다.
- FortiGate: REST API(토큰/계정) 또는 SSH. Palo Alto: SSH CLI(netmiko).
- 방화벽 추가 모달 + 수집(자격증명 입력) 모달 + 장비별 인터페이스/ARP 상세 뷰.
- 자격증명은 수집 시점에만 사용하고 저장하지 않습니다.

**보안**
- 방화벽 host는 등록·수집 양 시점 모두 허용 대역(allowed_ip_ranges)으로 검증, port는
  정수 1-65535로 강제(SSRF 방어, CWE-918). FortiGate 무검증 TLS는 경고 로깅.

## v3.1.4 (2026-06-29) — 실행 투명성 + 로컬 인증/UI 수정

**보안·투명성 (콘솔 표시 전환)**
- 배경: 이전 빌드는 `console=False`(windowed)라 콘솔 없이 백그라운드에서 서버를 열고
  브라우저를 자동 실행했습니다. 이 패턴은 백도어/드로퍼와 구분되지 않아 EDR·SmartScreen이
  차단할 수 있습니다.
- 수정: **`console=True`로 전환**하여 실행 시 콘솔 창이 보이고, 접속 주소·실행 모드·종료
  방법을 안내합니다. 프로세스가 가시적이고 사용자가 직접 제어(창 닫기=종료)할 수 있습니다.

**로컬 인증 수정 (수동 추가 등 'unauthorized' 해결)**
- 증상: 스위치 수동 추가 등 모든 작업에서 `unauthorized` 오류.
- 원인: production 모드가 모든 `/api/*`에 토큰을 강제했으나 로컬 UI는 토큰을 보내지 않음.
- 수정: **루프백 바인딩 + 로컬(127.0.0.1) 요청은 인증 면제**(같은 머신 사용자만 접근).
  외부 노출 바인딩(0.0.0.0 등)일 때만 토큰을 강제(원격 접근 방어).

**UI**
- 수동 추가 벤더 목록에서 Juniper 제거.

## v3.1.2 (2026-06-29) — 실행 UX 핫픽스

**더블클릭 실행 경험 개선**
- 증상: exe 실행 시 cmd 창이 깜빡이고, 화면(브라우저)이 자동으로 뜨지 않아 "실행 안 됨"으로 보임.
- 수정:
  - **브라우저 자동 오픈**: 서버 기동(포트 LISTEN)을 감지하면 기본 브라우저로 대시보드를 자동으로 연다.
  - **콘솔 깜빡 제거**: DB 권한 설정(icacls) 호출에 `CREATE_NO_WINDOW`를 적용하고, 동일 DB는 1회만 적용(매 DB 접근 시 콘솔이 깜빡이던 문제 해결).
  - **기동 실패 진단**: headless(console=False)라 안 보이던 오류를 `netdash_error.log`에 기록.

## v3.1.1 (2026-06-29) — 핫픽스

**실행 즉시 크래시 수정 (loopback 자동 토큰)**
- 증상: exe를 그냥 실행하면 `api_token is required in production mode` 오류로 크래시.
  (production 모드 기본 + API_TOKEN 미설정)
- 수정: 루프백 전용(127.0.0.1/localhost/::1) 바인딩이면 강한 API 토큰을 자동 생성·영속화
  (`netdash_token.txt`)하여 바로 구동. 외부 노출 바인딩(0.0.0.0 등)은 기존대로 토큰을
  명시 요구(CWE-306 유지).
- 영향: 폐쇄망 단일 호스트에서 더블클릭만으로 기동 가능. 보안 정책은 외부 바인딩에 한해 유지.

## v3.1.0 (2026-06-28)

**M9 — 엑셀 현황 보고서 빌더**
- 현재 DB 상태를 4시트 엑셀 보고서로 생성: 스위치 현황 / 포트 현황 / 이벤트 로그 /
  호스트 대장. 호스트 시트에는 M7 장부 대조 판정(verdict)을 함께 표기.
- `GET /api/report` 엔드포인트(xlsx 다운로드) + "보고서 내보내기" 버튼.
- 헤더 스타일·컬럼 너비 자동조정. 빈 DB도 헤더만 있는 정상 워크북 반환.

품질: 전체 테스트 114/114 통과(회귀 0). Opus 4.8 코드리뷰 PASS.

## v3.0.0 (2026-06-28)

M5~M8 네 개 마일스톤을 묶은 메이저 릴리스. 비동기 자격증명 보안 강화,
ExtremeXOS 파서 고도화, 그리고 장부 대조(Reconcile) 기능을 엔진부터 UI까지 완성.
(v2.0.0은 M4 릴리스가 점유하여 다음 메이저 v3.0.0으로 발행.)

### 주요 변경

**M5 — 비동기 자격증명 처리 (보안)**
- SSH 자격증명을 수집 큐 페이로드에서 제거하고 세션 저장소 경유로 전환 (CWE-522).
- 워커가 사용 시점에 로드하고 수집 종료(성공/실패/예외) 시 즉시 폐기.
- 세션 저장소 스레드 락 추가(CWE-362), 방어적 복사본 반환.

**M6 — ExtremeXOS 포트 표기 정규화 고도화**
- 잘못된 Cisco 'Gi' 접두사 제거, ExtremeXOS native 표기(slot:port / standalone) 보존.
- 가변 폭 Type 컬럼·들여쓰기·'Up-Link' 류 토큰에 견고한 토큰 기반 파싱.

**M7 — 장부 대조(Reconcile) 엔진**
- 엑셀 장부(기대 인벤토리) vs 실측(수집) 위치를 6판정으로 대조
  (일치/포트 불일치/스위치 불일치/장부에만/실측에만/정보없음).
- hosts 테이블 장부 컬럼 + 측정/장부 양방향 UPSERT 분리(적재 순서 무관 멱등).
- 비교용 포트 정규화(서브인터페이스 보존, 논리포트 충돌 방지, Juniper 커버).
- `GET /api/reconcile` 엔드포인트, 엑셀 장부 적재 경로(연결스위치/연결포트).

**M8 — 장부 대조 UI 패널**
- "장부 대조" 탭: 판정 요약 카드 + 호스트별 판정 테이블.
- 모든 동적 필드 XSS 이스케이프, 판정 배지 색상 구분.

### 품질
- 전체 테스트 106/106 통과 (회귀 0).
- 코드 리뷰: M5·M6 Codex, M7·M8 Claude Opus 4.8 (다중 라운드 PASS).
- Windows 단일 실행파일(netdash.exe) 빌드 + 스모크 테스트 통과.

### 배포
- `netdash.exe` (PyInstaller 단일 실행파일, console 비활성).
- 실행: 기본 `127.0.0.1:8082`. 데모 모드는 `DEMO_MODE=true` 환경변수.
- 프로덕션은 `API_TOKEN` 환경변수 필수.

---

## v2.0.0

- M4: 멀티블록 엑셀 로더 + 보안 하드닝 10종 (SSRF/DoS/메모리/권한).

## v1.0.0

- M1~M3: 기반 프레임워크, 수집·파싱, CLI/끊김탐지/DPAPI.
